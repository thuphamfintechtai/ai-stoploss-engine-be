---
phase: 04-risk-scenario-simulation
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - ai-stoploss-engine-fe/services/api.ts
  - ai-stoploss-engine-fe/components/RiskManagerView.tsx
autonomous: false
requirements:
  - RISK-01
  - RISK-02
  - RISK-03
  - RISK-04

must_haves:
  truths:
    - "User co the xem VaR: 'Voi 95% tin cay, max loss 1 ngay la X VND'"
    - "User co the xem Monte Carlo fan chart voi percentile bands"
    - "User co the chay stress test va thay impact len tung vi the"
    - "User co the xem sector concentration pie chart voi RED/YELLOW warnings"
  artifacts:
    - path: "ai-stoploss-engine-fe/services/api.ts"
      provides: "4 new API functions + TypeScript interfaces"
      contains: "getVaR"
    - path: "ai-stoploss-engine-fe/components/RiskManagerView.tsx"
      provides: "Tab-based risk simulation UI with VaR, Monte Carlo, Stress Test, Sector Concentration"
      contains: "riskSimTab"
  key_links:
    - from: "ai-stoploss-engine-fe/services/api.ts"
      to: "/api/ai/var"
      via: "apiClient.get"
      pattern: "apiClient\\.get.*ai/var"
    - from: "ai-stoploss-engine-fe/services/api.ts"
      to: "/api/ai/monte-carlo"
      via: "apiClient.post"
      pattern: "apiClient\\.post.*ai/monte-carlo"
    - from: "ai-stoploss-engine-fe/components/RiskManagerView.tsx"
      to: "ai-stoploss-engine-fe/services/api.ts"
      via: "import getVaR, getMonteCarloSimulation, etc"
      pattern: "import.*getVaR.*api"
---

<objective>
Frontend UI cho Risk Scenario Simulation: extend RiskManagerView voi 4 sections -- VaR card, Monte Carlo fan chart, Stress Test table, Sector Concentration pie chart.

Purpose: User xem risk metrics truc quan trong RiskManagerView -- tab/section layout de khong overload UI.
Output: RiskManagerView extended, 4 API functions in api.ts.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-risk-scenario-simulation/04-CONTEXT.md
@.planning/phases/04-risk-scenario-simulation/04-01-SUMMARY.md
@.planning/phases/04-risk-scenario-simulation/04-02-SUMMARY.md

<interfaces>
From ai-stoploss-engine-fe/services/api.ts (existing pattern):
```typescript
export interface RiskBudgetResult {
  usedRiskVnd: number;
  usedRiskPercent: number;
  maxRiskVnd: number;
  remainingBudget: number;
  positions: any[];
  sectorConcentration: any[];
}
export async function getRiskBudget(portfolioId: string): Promise<RiskBudgetResult> {
  const response = await apiClient.get('/ai/risk-budget', { params: { portfolio_id: portfolioId } });
  return response.data.data;
}
```

From ai-stoploss-engine-fe/components/RiskManagerView.tsx (existing):
```typescript
interface Props {
  portfolioId: string | null;
  positions: Position[];
  totalBalance: number;
  maxRiskPercent: number;
  onNavigate: (view: string) => void;
}
// Already has: RiskGauge component, Summary Bar, Exposure Chart, Risk Budget section, Position Risk Table
// Uses: Recharts (available), formatNumberVI, STOCK_PRICE_DISPLAY_SCALE
```

API endpoints (from Plan 02):
- GET /api/ai/var?portfolio_id=xxx -> { portfolioVaR, positionVaRs }
- POST /api/ai/monte-carlo { portfolio_id } -> { percentileBands, probabilityOfLoss, initialValue }
- POST /api/ai/stress-test { portfolio_id, custom_scenario? } -> { scenarios: [...] }
- GET /api/ai/sector-concentration?portfolio_id=xxx -> { sectors, warnings, totalPortfolioValue }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API functions + TypeScript interfaces in api.ts</name>
  <files>
    ai-stoploss-engine-fe/services/api.ts
  </files>
  <read_first>
    ai-stoploss-engine-fe/services/api.ts
  </read_first>
  <action>
    Them interfaces va functions vao api.ts (truoc export default apiClient):

    ```typescript
    // ─── Risk Simulation Types ──────────────────────────────────────────────────

    export interface VaRResult {
      portfolioVaR: {
        varVnd: number;
        varPercent: number;
        confidenceLevel: number;
        summary: string;
      };
      positionVaRs: Array<{
        symbol: string;
        varVnd: number;
        varPercent: number;
      }>;
    }

    export interface MonteCarloResult {
      percentileBands: {
        p5: number[];
        p25: number[];
        p50: number[];
        p75: number[];
        p95: number[];
      };
      probabilityOfLoss: number;
      initialValue: number;
    }

    export interface StressTestScenario {
      dropPercent: number;
      totalImpactVnd: number;
      totalImpactPercent: number;
      positions: Array<{
        symbol: string;
        sector: string;
        beta: number;
        positionValue: number;
        impactVnd: number;
        impactPercent: number;
      }>;
    }

    export interface StressTestResult {
      scenarios: StressTestScenario[];
    }

    export interface SectorConcentrationResult {
      sectors: Array<{
        sector: string;
        sectorLabel: string;
        totalValueVnd: number;
        percent: number;
        warningLevel: 'RED' | 'YELLOW' | 'GREEN';
      }>;
      warnings: string[];
      totalPortfolioValue: number;
    }

    // ─── Risk Simulation API ────────────────────────────────────────────────────

    export async function getVaR(portfolioId: string): Promise<VaRResult> {
      const response = await apiClient.get('/ai/var', { params: { portfolio_id: portfolioId } });
      return response.data.data;
    }

    export async function getMonteCarloSimulation(portfolioId: string): Promise<MonteCarloResult> {
      const response = await apiClient.post('/ai/monte-carlo', { portfolio_id: portfolioId });
      return response.data.data;
    }

    export async function getStressTest(portfolioId: string, customScenario?: number): Promise<StressTestResult> {
      const body: any = { portfolio_id: portfolioId };
      if (customScenario != null) body.custom_scenario = customScenario;
      const response = await apiClient.post('/ai/stress-test', body);
      return response.data.data;
    }

    export async function getSectorConcentration(portfolioId: string): Promise<SectorConcentrationResult> {
      const response = await apiClient.get('/ai/sector-concentration', { params: { portfolio_id: portfolioId } });
      return response.data.data;
    }
    ```
  </action>
  <verify>
    <automated>grep -c "getVaR\|getMonteCarloSimulation\|getStressTest\|getSectorConcentration" ai-stoploss-engine-fe/services/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export interface VaRResult" ai-stoploss-engine-fe/services/api.ts
    - grep -q "export interface MonteCarloResult" ai-stoploss-engine-fe/services/api.ts
    - grep -q "export interface StressTestResult" ai-stoploss-engine-fe/services/api.ts
    - grep -q "export interface SectorConcentrationResult" ai-stoploss-engine-fe/services/api.ts
    - grep -q "export async function getVaR" ai-stoploss-engine-fe/services/api.ts
    - grep -q "export async function getMonteCarloSimulation" ai-stoploss-engine-fe/services/api.ts
    - grep -q "export async function getStressTest" ai-stoploss-engine-fe/services/api.ts
    - grep -q "export async function getSectorConcentration" ai-stoploss-engine-fe/services/api.ts
  </acceptance_criteria>
  <done>4 typed API functions + 4 TypeScript interfaces added to api.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Extend RiskManagerView with VaR, Monte Carlo, Stress Test, Sector sections</name>
  <files>
    ai-stoploss-engine-fe/components/RiskManagerView.tsx
  </files>
  <read_first>
    ai-stoploss-engine-fe/components/RiskManagerView.tsx,
    ai-stoploss-engine-fe/services/api.ts
  </read_first>
  <action>
    Extend RiskManagerView.tsx -- them 4 sections moi SAU existing content (Risk Budget, Position Table). KHONG xoa bat ky UI nao da co.

    **1. Imports them:**
    ```typescript
    import { getVaR, getMonteCarloSimulation, getStressTest, getSectorConcentration } from '../services/api';
    import type { VaRResult, MonteCarloResult, StressTestResult, SectorConcentrationResult } from '../services/api';
    import { AreaChart, Area, XAxis, YAxis, Tooltip, ResponsiveContainer, PieChart, Pie, Cell } from 'recharts';
    ```

    **2. State them (trong component):**
    ```typescript
    const [riskSimTab, setRiskSimTab] = useState<'var' | 'montecarlo' | 'stress' | 'sector'>('var');
    const [varResult, setVarResult] = useState<VaRResult | null>(null);
    const [mcResult, setMcResult] = useState<MonteCarloResult | null>(null);
    const [stressResult, setStressResult] = useState<StressTestResult | null>(null);
    const [sectorResult, setSectorResult] = useState<SectorConcentrationResult | null>(null);
    const [simLoading, setSimLoading] = useState(false);
    const [customDrop, setCustomDrop] = useState<string>('');
    ```

    **3. useEffect fetch khi portfolioId thay doi:**
    - Goi getVaR + getSectorConcentration ngay (nhe)
    - KHONG goi Monte Carlo + Stress Test tu dong (nang) -- chi goi khi user click tab

    **4. Handler cho tab click:**
    ```typescript
    const handleSimTabChange = async (tab) => {
      setRiskSimTab(tab);
      if (tab === 'montecarlo' && !mcResult) { /* fetch */ }
      if (tab === 'stress' && !stressResult) { /* fetch */ }
    };
    ```

    **5. Custom stress test handler:**
    ```typescript
    const handleCustomStress = async () => {
      const drop = parseFloat(customDrop);
      if (isNaN(drop) || drop >= 0 || drop < -50) return;
      setSimLoading(true);
      const result = await getStressTest(portfolioId!, drop);
      setStressResult(result);
      setSimLoading(false);
    };
    ```

    **6. Section "Phan Tich Rui Ro Nang Cao" (sau Position Risk Table):**

    Panel voi tab bar: VaR | Monte Carlo | Stress Test | Sector

    **VaR Tab:**
    - Card lon voi summary sentence (per D-04): "Voi 95% tin cay, max loss 1 ngay la X VND (Y% portfolio)"
    - Table nho: per-position VaR (symbol, varVnd, varPercent)
    - Dung formatNumberVI cho so VND

    **Monte Carlo Tab:**
    - Fan chart (AreaChart tu Recharts):
      - X axis: Day 1-20
      - 5 areas stacked: p5 (do nhat), p25, p50 (median line dam), p75, p95 (xanh nhat)
      - Opacity gradient: p5=0.1, p25=0.2, p50 line, p75=0.2, p95=0.1
    - Summary: "Xac suat lo von: {probabilityOfLoss}%"
    - Loading state khi dang chay simulation

    **Stress Test Tab:**
    - 3 scenario cards (-10%, -15%, -20%) hien thi nhu grid
    - Moi card: totalImpactVnd (do), totalImpactPercent
    - Expandable: click card -> xem per-position impact table
    - Custom scenario: input field (-50 -> 0) + button "Chay"
    - Per D-13: Custom scenario result hien o cuoi

    **Sector Tab:**
    - PieChart (Recharts) voi colors per sector
    - COLORS = ['#3B82F6', '#EF4444', '#22C55E', '#F59E0B', '#8B5CF6', '#EC4899', '#06B6D4', '#F97316', '#6B7280']
    - Legend: sector name + percent + warning badge (RED/YELLOW)
    - Warning list o duoi (giong rebalancing warnings hien co)

    **UI Style:** Giong existing RiskManagerView -- panel-section, text-[11px] headers, text-text-muted, same color scheme. Tab bar dung buttons voi active state (border-b-2 border-accent).

    **Loading/Error states:**
    - Spinner giong existing pattern (w-3 h-3 border animate-spin)
    - "Khong du du lieu" khi API tra empty
    - KHONG block UI -- moi section fetch doc lap
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-fe && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "getVaR" ai-stoploss-engine-fe/components/RiskManagerView.tsx
    - grep -q "getMonteCarloSimulation" ai-stoploss-engine-fe/components/RiskManagerView.tsx
    - grep -q "getStressTest" ai-stoploss-engine-fe/components/RiskManagerView.tsx
    - grep -q "getSectorConcentration" ai-stoploss-engine-fe/components/RiskManagerView.tsx
    - grep -q "AreaChart\|PieChart" ai-stoploss-engine-fe/components/RiskManagerView.tsx
    - grep -q "riskSimTab" ai-stoploss-engine-fe/components/RiskManagerView.tsx
    - grep -q "customDrop\|custom_scenario\|customStress" ai-stoploss-engine-fe/components/RiskManagerView.tsx
    - TypeScript compiles without errors (npx tsc --noEmit)
  </acceptance_criteria>
  <done>
    RiskManagerView has 4 new sub-sections with tab navigation. VaR shows summary + per-position table. Monte Carlo shows fan chart. Stress Test shows scenario cards with expandable details + custom scenario. Sector shows pie chart with warnings. All sections load independently, TypeScript compiles clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Risk Simulation UI</name>
  <files>ai-stoploss-engine-fe/components/RiskManagerView.tsx</files>
  <action>
    Human verify Risk Scenario Simulation UI da duoc them vao RiskManagerView:
    1. VaR card voi "Voi 95% tin cay, max loss 1 ngay la X VND"
    2. Monte Carlo fan chart 20 ngay
    3. Stress Test scenarios -10/-15/-20% + custom
    4. Sector Concentration pie chart voi warnings

    Steps:
    1. Start backend: cd ai-stoploss-engine-be and npm run dev
    2. Start frontend: cd ai-stoploss-engine-fe and npm run dev
    3. Login va chon portfolio co it nhat 2-3 vi the mo
    4. Navigate den Risk Manager view
    5. Verify: Existing Risk Budget + Position Table van hien thi dung
    6. Scroll xuong section "Phan Tich Rui Ro Nang Cao"
    7. Click tab VaR -- xem summary sentence + per-position table
    8. Click tab Monte Carlo -- doi fan chart load, xem probability of loss
    9. Click tab Stress Test -- xem 3 scenario cards, thu nhap custom (-25) va click Chay
    10. Click tab Sector -- xem pie chart + warning list
    11. Kiem tra responsive: thu resize browser nho lai
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-fe && npx tsc --noEmit</automated>
  </verify>
  <done>User confirms all 4 risk simulation tabs work correctly, data loads, charts render, existing UI untouched.</done>
</task>

</tasks>

<verification>
1. npx tsc --noEmit trong fe directory -- no errors
2. Manual test: login, navigate to Risk Manager, check 4 tabs
3. Backend tests: cd ai-stoploss-engine-be && npx vitest run tests/varService.test.js tests/monteCarloService.test.js tests/stressTestService.test.js tests/sectorConcentration.test.js
</verification>

<success_criteria>
- User xem duoc VaR summary "Voi 95% tin cay..." -- RISK-01 complete
- User xem duoc Monte Carlo fan chart voi percentile bands -- RISK-02 complete
- User chay duoc stress test 3 scenarios + custom -- RISK-03 complete
- User xem duoc sector pie chart voi RED/YELLOW warnings -- RISK-04 complete
- Existing RiskManagerView content (Risk Budget, Position Table) khong bi thay doi
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-risk-scenario-simulation/04-03-SUMMARY.md`
</output>
