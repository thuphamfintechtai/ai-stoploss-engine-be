---
phase: 02-paper-trading-engine
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - ai-stoploss-engine-fe/services/api.ts
  - ai-stoploss-engine-fe/components/PortfolioView.tsx
  - ai-stoploss-engine-fe/components/PaperPerformanceReport.tsx
  - ai-stoploss-engine-fe/components/PaperOrderManager.tsx
  - ai-stoploss-engine-fe/components/PaperVirtualBalance.tsx
autonomous: false
requirements:
  - PAPER-01
  - PAPER-02
  - PAPER-03
  - PAPER-04
  - PAPER-06
must_haves:
  truths:
    - "User co the dat lenh REALISTIC (chon dropdown) va thay order PENDING cho khop"
    - "User co the xem virtual balance rieng cho paper trading"
    - "User co the sua limit_price/quantity cho lenh PENDING qua UI"
    - "User co the xem performance report voi P&L, win rate, profit factor"
    - "User co the filter report theo tuan/thang/tong cong"
    - "Paper trading tab hien thi tat ca features moi"
  artifacts:
    - path: "ai-stoploss-engine-fe/services/api.ts"
      provides: "API client functions cho paper trading endpoints"
      contains: "paperPerformance"
    - path: "ai-stoploss-engine-fe/components/PaperPerformanceReport.tsx"
      provides: "Performance report component voi metrics + buy & hold"
      min_lines: 80
    - path: "ai-stoploss-engine-fe/components/PaperOrderManager.tsx"
      provides: "Order edit/cancel UI cho PENDING orders"
      min_lines: 60
    - path: "ai-stoploss-engine-fe/components/PaperVirtualBalance.tsx"
      provides: "Virtual balance display component"
      min_lines: 40
  key_links:
    - from: "api.ts"
      to: "backend API"
      via: "axios calls to paper trading endpoints"
      pattern: "paper-performance|orders.*PATCH"
    - from: "PortfolioView.tsx"
      to: "PaperPerformanceReport.tsx"
      via: "render in Paper Trading tab"
      pattern: "PaperPerformanceReport"
    - from: "PortfolioView.tsx"
      to: "PaperVirtualBalance.tsx"
      via: "render virtual balance in Paper tab header"
      pattern: "PaperVirtualBalance"
---

<objective>
Wire frontend Paper Trading tab voi tat ca backend features moi: REALISTIC mode toggle, virtual balance display, order edit UI, va performance report.

Purpose: User can tuong tac voi Paper Trading Engine qua UI. Day la plan cuoi cua Phase 2, wire tat ca backend work (Plan 01-04) vao frontend.

Output: API client functions, 3 new components, updated PortfolioView.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-paper-trading-engine/02-CONTEXT.md
@.planning/phases/02-paper-trading-engine/02-01-SUMMARY.md
@.planning/phases/02-paper-trading-engine/02-02-SUMMARY.md
@.planning/phases/02-paper-trading-engine/02-03-SUMMARY.md
@.planning/phases/02-paper-trading-engine/02-04-SUMMARY.md

<interfaces>
<!-- Backend API endpoints from Plans 01-04 -->

POST /api/portfolios/:portfolioId/orders
  Body: { symbol, exchange, side, order_type, limit_price, quantity, simulation_mode: 'INSTANT'|'REALISTIC', ... }
  Response: { success, data: { order, position? }, warnings? }

PATCH /api/portfolios/:portfolioId/orders/:id
  Body: { limit_price?, quantity? }
  Response: { success, data: updatedOrder }

DELETE /api/portfolios/:portfolioId/orders/:id
  Response: { success, data: cancelledOrder }

GET /api/portfolios/:portfolioId/paper-performance?period=all|week|month
  Response: { success, data: { total_trades, winning_trades, losing_trades, win_rate, total_pnl, avg_win, avg_loss, profit_factor, max_drawdown_vnd, max_drawdown_pct, buy_hold: { return, return_pct, value, cost } } }

GET /api/portfolios/:portfolioId/virtual-balance (hoac embedded in portfolio response)
  Response: { virtual_balance, paper_available_cash, paper_pending_settlement, paper_deployed }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API client + 3 new components</name>
  <files>
    ai-stoploss-engine-fe/services/api.ts,
    ai-stoploss-engine-fe/components/PaperVirtualBalance.tsx,
    ai-stoploss-engine-fe/components/PaperOrderManager.tsx,
    ai-stoploss-engine-fe/components/PaperPerformanceReport.tsx
  </files>
  <read_first>
    ai-stoploss-engine-fe/services/api.ts,
    ai-stoploss-engine-fe/components/PortfolioView.tsx,
    ai-stoploss-engine-fe/services/websocket.ts
  </read_first>
  <action>
    1. Them API functions vao api.ts:
       - editPaperOrder(portfolioId, orderId, data: { limit_price?, quantity? }): PATCH /orders/:id
       - getPaperPerformance(portfolioId, period = 'all'): GET /paper-performance?period=
       - getPaperVirtualBalance(portfolioId): GET endpoint hoac extract tu portfolio response
       - Check existing portfolio API response -- virtual balance fields co the da co trong portfolio object sau migration

    2. Tao PaperVirtualBalance.tsx:
       - Props: { portfolioId: string }
       - Fetch virtual balance on mount (hoac nhan tu parent)
       - Hien thi card voi 4 metrics:
         * Tong von ao: virtual_balance (format VND)
         * Kha dung: paper_available_cash
         * Cho thanh toan T+2: paper_pending_settlement
         * Da deploy: paper_deployed
       - Tailwind: card shadow, grid 2x2, green/blue/yellow/gray colors
       - Auto-refresh khi nhan WebSocket 'order_filled' event

    3. Tao PaperOrderManager.tsx:
       - Props: { portfolioId: string, orders: Order[], onRefresh: () => void }
       - Hien thi danh sach PENDING orders (filtered)
       - Moi order row co:
         * Symbol, Side, Type, Quantity, Limit Price, Status, Placed At
         * Edit button -> modal/inline form cho limit_price + quantity
         * Cancel button -> confirm dialog -> DELETE /orders/:id
       - Edit form: input cho limit_price va quantity, submit goi editPaperOrder
       - Sau edit/cancel thanh cong: goi onRefresh()
       - Tailwind: table layout, hover states, edit modal voi backdrop

    4. Tao PaperPerformanceReport.tsx:
       - Props: { portfolioId: string }
       - Fetch performance on mount
       - Period selector: tabs hoac dropdown (All / This Week / This Month)
       - Metrics display (per D-11):
         * Total P&L (green/red), Win Rate %, Average Win, Average Loss
         * Profit Factor, Max Drawdown
       - Buy & Hold comparison section (per D-12):
         * "Neu ban giu tat ca co phieu: +X VND (Y%)"
         * "Thuc te ban trade: +Z VND"
         * So sanh visual (bar chart hoac simple comparison card)
       - Tailwind: metric cards grid, color-coded positive/negative
       - Recharts (da co) cho equity curve hoac simple bar comparison
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-fe && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "editPaperOrder" ai-stoploss-engine-fe/services/api.ts
    - grep -q "getPaperPerformance" ai-stoploss-engine-fe/services/api.ts
    - grep -q "PaperVirtualBalance" ai-stoploss-engine-fe/components/PaperVirtualBalance.tsx
    - grep -q "PaperOrderManager" ai-stoploss-engine-fe/components/PaperOrderManager.tsx
    - grep -q "PaperPerformanceReport" ai-stoploss-engine-fe/components/PaperPerformanceReport.tsx
    - grep -q "profit_factor\|profitFactor" ai-stoploss-engine-fe/components/PaperPerformanceReport.tsx
    - grep -q "win_rate\|winRate" ai-stoploss-engine-fe/components/PaperPerformanceReport.tsx
    - grep -q "buy.*hold\|buyHold" ai-stoploss-engine-fe/components/PaperPerformanceReport.tsx
  </acceptance_criteria>
  <done>
    3 new components created: PaperVirtualBalance, PaperOrderManager, PaperPerformanceReport.
    API client functions added. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire components vao PortfolioView Paper Trading tab</name>
  <files>
    ai-stoploss-engine-fe/components/PortfolioView.tsx
  </files>
  <read_first>
    ai-stoploss-engine-fe/components/PortfolioView.tsx,
    ai-stoploss-engine-fe/components/PaperVirtualBalance.tsx,
    ai-stoploss-engine-fe/components/PaperOrderManager.tsx,
    ai-stoploss-engine-fe/components/PaperPerformanceReport.tsx
  </read_first>
  <action>
    1. Doc PortfolioView.tsx de hieu current Paper Trading tab structure
    2. Import 3 new components
    3. Trong Paper Trading tab section:
       a. Them PaperVirtualBalance o dau tab (header area)
       b. Them toggle/dropdown cho simulation_mode (INSTANT/REALISTIC) khi dat lenh
          - Default: REALISTIC (new behavior)
          - Pass simulation_mode vao order form request body
       c. Them PaperOrderManager hien thi PENDING orders voi edit/cancel
       d. Them PaperPerformanceReport section (co the la sub-tab hoac section duoi)
    4. Ensure WebSocket listener update virtual balance khi order filled
    5. Ensure order list refresh khi edit/cancel/fill xay ra
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-fe && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "PaperVirtualBalance" ai-stoploss-engine-fe/components/PortfolioView.tsx
    - grep -q "PaperOrderManager" ai-stoploss-engine-fe/components/PortfolioView.tsx
    - grep -q "PaperPerformanceReport" ai-stoploss-engine-fe/components/PortfolioView.tsx
    - grep -q "REALISTIC" ai-stoploss-engine-fe/components/PortfolioView.tsx
  </acceptance_criteria>
  <done>
    Paper Trading tab hien thi virtual balance, order manager, va performance report.
    User co the chon INSTANT/REALISTIC mode khi dat lenh.
    All components wired va TypeScript compiles.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification -- Full Paper Trading Engine E2E</name>
  <files>none</files>
  <action>
    Verify full Paper Trading Engine hoat dong end-to-end qua UI.
  </action>
  <what-built>
    Full Paper Trading Engine: matching engine realistic voi slippage + fill probability, virtual balance rieng, order edit/cancel, T+2 settlement, va performance report. Frontend wired voi tat ca backend features.
  </what-built>
  <how-to-verify>
    1. Start backend: cd ai-stoploss-engine-be && npm run dev
    2. Start frontend: cd ai-stoploss-engine-fe && npm run dev
    3. Chay migration: psql -f ai-stoploss-engine-be/migrations/008_paper_virtual_balance.sql
    4. Vao Paper Trading tab:
       a. Check virtual balance hien thi 1,000,000,000 VND
       b. Dat lenh BUY MP (REALISTIC mode): confirm order filled voi slippage > 0
       c. Dat lenh BUY LO (REALISTIC mode): confirm order PENDING, cho worker fill
       d. Sua limit_price lenh PENDING: confirm updated
       e. Huy lenh PENDING: confirm cancelled, virtual balance duoc refund
       f. Check virtual balance giam sau khi lenh filled
       g. Xem Performance Report: confirm metrics hien thi (hoac "Chua co giao dich" neu moi)
       h. Run tests: cd ai-stoploss-engine-be && npx vitest run
  </how-to-verify>
  <verify>
    <automated>cd ai-stoploss-engine-be && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>User confirmed Paper Trading Engine hoat dong dung end-to-end.</done>
  <resume-signal>Type "approved" hoac mo ta issues can fix</resume-signal>
</task>

</tasks>

<verification>
cd ai-stoploss-engine-fe && npx tsc --noEmit && cd ../ai-stoploss-engine-be && npx vitest run --reporter=verbose
</verification>

<success_criteria>
- User co the dat lenh REALISTIC (PAPER-01, PAPER-02)
- Virtual balance hien thi va update khi trade (PAPER-03)
- User co the edit/cancel pending orders qua UI (PAPER-04)
- Performance report hien thi metrics + buy & hold (PAPER-06)
- Human verification passed
</success_criteria>

<output>
After completion, create `.planning/phases/02-paper-trading-engine/02-05-SUMMARY.md`
</output>
