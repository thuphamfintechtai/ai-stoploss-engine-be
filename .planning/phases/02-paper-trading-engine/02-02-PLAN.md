---
phase: 02-paper-trading-engine
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - ai-stoploss-engine-be/services/paper/fillEngine.js
  - ai-stoploss-engine-be/workers/paperFillWorker.js
  - ai-stoploss-engine-be/index.js
  - ai-stoploss-engine-be/controllers/paper/paperOrder.controller.js
  - ai-stoploss-engine-be/tests/services/paperFillEngine.test.js
autonomous: true
requirements:
  - PAPER-01
  - PAPER-02
  - PAPER-05
must_haves:
  truths:
    - "REALISTIC mode: MP fill ngay voi slippage (qua PaperMatchingEngine), LO vao queue PENDING cho worker"
    - "INSTANT mode: van hoat dong nhu cu (backward compatible)"
    - "PaperFillWorker chay moi 30s, check PENDING PAPER orders va try fill qua PaperMatchingEngine"
    - "Khi order filled: PaperCapitalService.deductForBuy duoc goi de giam virtual balance"
    - "Khi order filled via SELL: addPendingSettlement duoc goi voi T+2"
    - "Worker registered trong index.js voi node-cron"
    - "Delay 1-5s cho LO fill (per D-04) duoc mo phong trong worker"
  artifacts:
    - path: "ai-stoploss-engine-be/services/paper/fillEngine.js"
      provides: "Upgraded fill engine voi REALISTIC mode support"
      exports: ["fillOrderInstant", "fillOrderRealistic", "expireEndOfSessionOrders"]
    - path: "ai-stoploss-engine-be/workers/paperFillWorker.js"
      provides: "Periodic worker cho PENDING PAPER orders"
      exports: ["startPaperFillWorker"]
      min_lines: 60
  key_links:
    - from: "fillEngine.js"
      to: "paperMatchingEngine.js"
      via: "import PaperMatchingEngine for slippage/fill calc"
      pattern: "PaperMatchingEngine"
    - from: "fillEngine.js"
      to: "paperCapitalService.js"
      via: "deductForBuy khi order filled"
      pattern: "PaperCapitalService"
    - from: "paperFillWorker.js"
      to: "fillEngine.js"
      via: "call fillOrderRealistic for each pending order"
      pattern: "fillOrderRealistic"
    - from: "index.js"
      to: "paperFillWorker.js"
      via: "startPaperFillWorker() registration"
      pattern: "startPaperFillWorker"
---

<objective>
Upgrade fillEngine.js de support REALISTIC mode (slippage + fill probability) va tao PaperFillWorker chay periodic de fill PENDING LO orders.

Purpose: Chuyen tu INSTANT fill (100%, khong slippage) sang matching engine realistic. Worker chay moi 30s check pending orders, dung PaperMatchingEngine de quyet dinh fill hay khong. Virtual balance duoc deduct khi order filled.

Output: fillEngine.js upgraded, paperFillWorker.js moi, worker registered trong index.js, controller updated cho REALISTIC flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-paper-trading-engine/02-CONTEXT.md
@.planning/phases/02-paper-trading-engine/02-RESEARCH.md
@.planning/phases/02-paper-trading-engine/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From ai-stoploss-engine-be/services/paper/paperMatchingEngine.js:
```javascript
class PaperMatchingEngine {
  static calculateSlippage(price, quantity, avgDailyVolume, exchange) { ... }
  static calculateFillProbability(orderQty, avgDailyVolume) { ... }
  static async tryFillLimitOrder(order, currentPrice, avgDailyVolume) { ... }
  static async fillMarketOrder(order, currentPrice, avgDailyVolume, exchange) { ... }
  static async fillATOOrder(order, openPrice) { ... }
  static async fillATCOrder(order, closePrice) { ... }
}
```

From ai-stoploss-engine-be/services/paper/paperCapitalService.js:
```javascript
class PaperCapitalService {
  static async deductForBuy(portfolioId, totalCost) { ... }
  static async addPendingSettlement(portfolioId, netAmount, orderId) { ... }
  static async processSettlements() { ... }
  static async getVirtualBalance(portfolioId) { ... }
  static async refundForCancel(portfolioId, amount) { ... }
}
```

From ai-stoploss-engine-be/services/paper/fillEngine.js (current):
```javascript
export async function fillOrderInstant(order, portfolio) { ... }
export async function expireEndOfSessionOrders() { ... }
```

From ai-stoploss-engine-be/models/Order.js:
```javascript
static async fill(id, fillQty, fillPriceVnd) { ... } // optimistic locking
static async findPendingByPortfolio(portfolioId) { ... }
static async getDistinctPendingSymbols() { ... }
static async findPendingBySymbol(symbol) { ... }
```

From ai-stoploss-engine-be/workers/stopLossMonitor.js:
```javascript
// Reference pattern for worker structure with node-cron
export function startWorker() { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade fillEngine.js + tao fillOrderRealistic function</name>
  <files>
    ai-stoploss-engine-be/services/paper/fillEngine.js,
    ai-stoploss-engine-be/controllers/paper/paperOrder.controller.js,
    ai-stoploss-engine-be/tests/services/paperFillEngine.test.js
  </files>
  <read_first>
    ai-stoploss-engine-be/services/paper/fillEngine.js,
    ai-stoploss-engine-be/controllers/paper/paperOrder.controller.js,
    ai-stoploss-engine-be/services/paper/paperMatchingEngine.js,
    ai-stoploss-engine-be/services/paper/paperCapitalService.js,
    ai-stoploss-engine-be/services/shared/feeEngine.js,
    ai-stoploss-engine-be/workers/stopLossMonitor.js
  </read_first>
  <action>
    1. Upgrade fillEngine.js:
       a. Import PaperMatchingEngine va PaperCapitalService
       b. Giu nguyen fillOrderInstant cho INSTANT mode (backward compatible)
       c. THEM goi PaperCapitalService.deductForBuy vao fillOrderInstant truoc khi tao position (per Pitfall 4):
          - totalCost = fillPrice * quantity + buyFeeVnd
          - Wrap trong try/catch: neu khong du tien -> reject order (Order.reject)
       d. Tao fillOrderRealistic(order, portfolio = null) function moi:
          - Context guard: chi PAPER orders
          - Lay currentPrice qua getCurrentPriceVnd (reuse existing helper)
          - Lay avgDailyVolume: fetch 20 candles, tinh average volume, cache 1 gio (simple Map cache)
          - Switch theo order_type:
            * MP: PaperMatchingEngine.fillMarketOrder() -> fill ngay voi slippage
            * LO: PaperMatchingEngine.tryFillLimitOrder() -> co the filled hoac khong
            * ATO: Lay open price tu candle, PaperMatchingEngine.fillATOOrder()
            * ATC: Lay close price tu candle, PaperMatchingEngine.fillATCOrder()
          - Neu filled:
            * Tinh buyFeeVnd
            * PaperCapitalService.deductForBuy(portfolioId, fillPrice * qty + buyFeeVnd)
            * Order.fill(id, qty, fillPrice)
            * Tao position (same logic nhu fillOrderInstant)
            * ExecutionLog + broadcastPortfolioUpdate
          - Neu SELL filled:
            * Tinh sellFees qua calculateFees
            * PaperCapitalService.addPendingSettlement(portfolioId, netAmount, orderId)
       e. Export: fillOrderInstant, fillOrderRealistic, expireEndOfSessionOrders

    2. Update paperOrder.controller.js createOrder function:
       - Khi simulation_mode === 'REALISTIC':
         * MP: goi fillOrderRealistic (fill ngay voi slippage, khong delay)
         * LO/ATO/ATC: chi tao order PENDING, KHONG fill (worker se fill)
       - Khi simulation_mode === 'INSTANT': giu nguyen behavior hien tai (fillOrderInstant)
       - Response message: "Lenh da khop voi slippage X VND" (REALISTIC MP) hoac "Lenh da dat, cho khop (REALISTIC)" cho LO

    3. Tao helper function getAvgDailyVolume(symbol, exchange):
       - Fetch 20 candles via market API
       - Tinh avg volume = sum(volumes) / count
       - Cache in-memory Map voi TTL 1 gio
       - Return number (default 100000 neu fetch fail -- medium liquidity fallback)
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-be && npx vitest run tests/services/paperFillEngine.test.js --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "fillOrderRealistic" ai-stoploss-engine-be/services/paper/fillEngine.js
    - grep -q "PaperMatchingEngine" ai-stoploss-engine-be/services/paper/fillEngine.js
    - grep -q "PaperCapitalService" ai-stoploss-engine-be/services/paper/fillEngine.js
    - grep -q "deductForBuy" ai-stoploss-engine-be/services/paper/fillEngine.js
    - grep -q "REALISTIC" ai-stoploss-engine-be/controllers/paper/paperOrder.controller.js
    - grep -q "fillOrderRealistic" ai-stoploss-engine-be/controllers/paper/paperOrder.controller.js
    - grep -q "getAvgDailyVolume" ai-stoploss-engine-be/services/paper/fillEngine.js
  </acceptance_criteria>
  <done>
    fillEngine.js co fillOrderRealistic dung PaperMatchingEngine cho slippage + fill probability.
    PaperCapitalService.deductForBuy duoc goi khi order filled (ca INSTANT va REALISTIC).
    Controller route REALISTIC MP qua fillOrderRealistic, REALISTIC LO/ATO/ATC chi tao PENDING.
    INSTANT mode van hoat dong backward compatible.
  </done>
</task>

<task type="auto">
  <name>Task 2: PaperFillWorker -- periodic fill check cho PENDING orders</name>
  <files>
    ai-stoploss-engine-be/workers/paperFillWorker.js,
    ai-stoploss-engine-be/index.js
  </files>
  <read_first>
    ai-stoploss-engine-be/workers/stopLossMonitor.js,
    ai-stoploss-engine-be/workers/settlementWorker.js,
    ai-stoploss-engine-be/index.js,
    ai-stoploss-engine-be/services/paper/fillEngine.js
  </read_first>
  <action>
    1. Tao workers/paperFillWorker.js (per D-04, Research Pattern 3):
       - import cron from 'node-cron'
       - import Order from '../models/Order.js'
       - import { fillOrderRealistic } from '../services/paper/fillEngine.js'
       - import { expireEndOfSessionOrders } from '../services/paper/fillEngine.js'

       - FILL_CRON = '*/30 * * * * *' (moi 30 giay)
       - EXPIRE_CRON = '0 15 * * 1-5' (15:00 Mon-Fri, het phien)
       - SETTLEMENT_CRON = '0 9 * * 1-5' (9:00 Mon-Fri, xu ly paper settlement)

       - async function checkPendingOrders():
         * Query PAPER PENDING orders: SELECT * FROM financial.orders WHERE context = 'PAPER' AND status = 'PENDING' AND simulation_mode = 'REALISTIC'
         * Group by symbol
         * For each group:
           - For each order: await fillOrderRealistic(order)
           - Delay random 1-5s giua cac fills (per D-04): await new Promise(r => setTimeout(r, 1000 + Math.random() * 4000))
         * Log summary: "[PaperFillWorker] Checked X pending orders, Y filled"

       - async function processExpiry():
         * Goi expireEndOfSessionOrders()
         * ATO orders: check expired_at (09:14:59), ATC orders: check expired_at (15:00:00)
         * Per D-10: ATO expire neu khong khop trong phien mo cua, ATC expire phien dong

       - async function processPaperSettlements():
         * Import PaperCapitalService
         * Goi PaperCapitalService.processSettlements()

       - Market hours guard (copy pattern tu stopLossMonitor):
         * Chi chay fill check trong gio giao dich VN: 9:00-11:30, 13:00-15:00 (GMT+7)
         * Ngoai gio: skip, log "[PaperFillWorker] Outside trading hours, skip"

       - export function startPaperFillWorker():
         * cron.schedule(FILL_CRON, checkPendingOrders)
         * cron.schedule(EXPIRE_CRON, processExpiry)
         * cron.schedule(SETTLEMENT_CRON, processPaperSettlements)
         * console.log('[PaperFillWorker] Started: fill check */30s, expire 15:00, settlement 09:00')

    2. Register trong index.js:
       - import { startPaperFillWorker } from './workers/paperFillWorker.js'
       - Them sau startSettlementWorker():
         console.log('Paper Fill Worker starting...')
         startPaperFillWorker()
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-be && grep -q "startPaperFillWorker" index.js && grep -q "fillOrderRealistic" workers/paperFillWorker.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "startPaperFillWorker" ai-stoploss-engine-be/workers/paperFillWorker.js
    - grep -q "checkPendingOrders" ai-stoploss-engine-be/workers/paperFillWorker.js
    - grep -q "REALISTIC" ai-stoploss-engine-be/workers/paperFillWorker.js
    - grep -q "fillOrderRealistic" ai-stoploss-engine-be/workers/paperFillWorker.js
    - grep -q "processSettlements" ai-stoploss-engine-be/workers/paperFillWorker.js
    - grep -q "startPaperFillWorker" ai-stoploss-engine-be/index.js
    - grep -q "cron.schedule" ai-stoploss-engine-be/workers/paperFillWorker.js
  </acceptance_criteria>
  <done>
    PaperFillWorker chay 3 cron jobs: fill check moi 30s (chi trading hours), expire 15:00, settlement 09:00.
    Worker registered trong index.js.
    PENDING REALISTIC orders duoc fill boi worker voi delay 1-5s (D-04).
    Paper settlements duoc process T+2 (D-06).
  </done>
</task>

</tasks>

<verification>
cd ai-stoploss-engine-be && npx vitest run --reporter=verbose && grep -q "startPaperFillWorker" index.js
</verification>

<success_criteria>
- fillEngine.js co REALISTIC mode voi PaperMatchingEngine integration
- PaperCapitalService.deductForBuy duoc goi khi order filled (Pitfall 4 fixed)
- PaperFillWorker chay periodic voi market hours guard
- Worker registered trong index.js
- All existing tests van pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-paper-trading-engine/02-02-SUMMARY.md`
</output>
