---
phase: 02-paper-trading-engine
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - ai-stoploss-engine-be/controllers/paper/paperOrder.controller.js
  - ai-stoploss-engine-be/routes/orders.routes.js
  - ai-stoploss-engine-be/tests/services/paperOrderManagement.test.js
autonomous: true
requirements:
  - PAPER-04
must_haves:
  truths:
    - "User co the sua limit_price va quantity cho lenh PENDING (per D-09)"
    - "User KHONG the sua side hay symbol (per D-09)"
    - "Sua limit_price phai pass tick size validation va price band check"
    - "Sua quantity phai la boi so cua 100 (lot size)"
    - "Chi sua duoc lenh PENDING, khong sua FILLED/CANCELLED/EXPIRED"
    - "Khi edit quantity tang: check virtual balance du de cover cost moi"
    - "Khi cancel order co deductForBuy: refund virtual balance"
  artifacts:
    - path: "ai-stoploss-engine-be/controllers/paper/paperOrder.controller.js"
      provides: "editOrder endpoint"
      exports: ["editOrder", "editOrderSchema"]
    - path: "ai-stoploss-engine-be/routes/orders.routes.js"
      provides: "PATCH route cho order edit"
      contains: "router.patch"
  key_links:
    - from: "orders.routes.js"
      to: "paperOrder.controller.js"
      via: "PATCH /:id route"
      pattern: "router.patch.*editOrder"
    - from: "paperOrder.controller.js"
      to: "paperCapitalService.js"
      via: "refundForCancel khi cancel, adjust khi edit quantity"
      pattern: "refundForCancel"
---

<objective>
Them PATCH endpoint de user sua lenh PENDING (limit_price + quantity) va integrate virtual balance refund khi cancel order.

Purpose: Per D-09, user can sua limit price va quantity cho lenh dang cho khop. Phan nay cung fix cancel flow de refund virtual balance khi user huy lenh da deduct tien.

Output: editOrder controller + schema, PATCH route, cancel flow updated voi refund, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-paper-trading-engine/02-CONTEXT.md
@.planning/phases/02-paper-trading-engine/02-01-SUMMARY.md

<interfaces>
From ai-stoploss-engine-be/services/paper/paperCapitalService.js:
```javascript
class PaperCapitalService {
  static async deductForBuy(portfolioId, totalCost) { ... }
  static async refundForCancel(portfolioId, amount) { ... }
  static async getVirtualBalance(portfolioId) { ... }
}
```

From ai-stoploss-engine-be/controllers/paper/paperOrder.controller.js:
```javascript
export const createOrder = async (req, res, next) => { ... }
export const listOrders = async (req, res, next) => { ... }
export const cancelOrder = async (req, res, next) => { ... }
export const createOrderSchema = Joi.object({ ... })
```

From ai-stoploss-engine-be/models/Order.js:
```javascript
static async findById(id) { ... }
static async cancel(id) { ... } // optimistic locking
```

From ai-stoploss-engine-be/services/shared/tickSizeEngine.js:
```javascript
export function snapToTickSize(price, exchange) { ... }
export function validatePriceInBand(price, refPrice, exchange) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PATCH /orders/:id endpoint + cancel refund</name>
  <files>
    ai-stoploss-engine-be/controllers/paper/paperOrder.controller.js,
    ai-stoploss-engine-be/routes/orders.routes.js,
    ai-stoploss-engine-be/tests/services/paperOrderManagement.test.js
  </files>
  <read_first>
    ai-stoploss-engine-be/controllers/paper/paperOrder.controller.js,
    ai-stoploss-engine-be/routes/orders.routes.js,
    ai-stoploss-engine-be/models/Order.js,
    ai-stoploss-engine-be/services/paper/paperCapitalService.js,
    ai-stoploss-engine-be/services/shared/tickSizeEngine.js,
    ai-stoploss-engine-be/middleware/validation.js,
    ai-stoploss-engine-be/services/shared/feeEngine.js
  </read_first>
  <behavior>
    - editOrder: PATCH voi valid limit_price -> order updated, return 200
    - editOrder: PATCH voi valid quantity -> order updated, return 200
    - editOrder: PATCH voi quantity > current AND khong du virtual balance -> return 422
    - editOrder: PATCH lenh khong PENDING -> return 409
    - editOrder: PATCH voi side field -> return 400 (khong cho sua side)
    - editOrder: PATCH voi symbol field -> return 400 (khong cho sua symbol)
    - editOrder: limit_price auto-snapped to tick size
    - cancelOrder: refund virtual balance sau khi cancel thanh cong
  </behavior>
  <action>
    1. Them editOrderSchema vao paperOrder.controller.js:
       ```javascript
       export const editOrderSchema = Joi.object({
         limit_price: Joi.number().positive().optional(),
         quantity: Joi.number().integer().positive().min(100).optional(),
       }).min(1); // it nhat 1 field
       ```
       KHONG cho phep side, symbol, order_type trong schema (per D-09).

    2. Them editOrder controller function:
       - ensurePortfolioOwnership
       - const order = await Order.findById(req.params.id)
       - Check order.portfolio_id === portfolio.id
       - Check order.status === 'PENDING' (409 neu khong)
       - Check order.context === 'PAPER' (chi edit paper orders)

       - Neu co limit_price:
         * snapToTickSize(limit_price, order.exchange)
         * validatePriceInBand -- warning only, khong block
         * Validate SL/TP van hop le voi gia moi (neu order co SL/TP)

       - Neu co quantity:
         * Check quantity % 100 === 0 (lot size validation)
         * Neu quantity > order.quantity (tang): check virtual balance du cover delta cost
           - deltaCost = (newQty - oldQty) * order.limit_price + fee delta
           - PaperCapitalService.getVirtualBalance -> check paper_available_cash >= deltaCost
           - Neu du: PaperCapitalService.deductForBuy(portfolioId, deltaCost)
         * Neu quantity < order.quantity (giam): refund delta
           - PaperCapitalService.refundForCancel(portfolioId, refundAmount)

       - UPDATE order: SET limit_price = $2, quantity = $3 WHERE id = $1 AND status = 'PENDING'
         (optimistic locking -- return null neu da bi fill)
       - ExecutionLog write ORDER_MODIFIED event
       - Return 200 voi updated order

    3. Them Order.edit static method vao Order model (NEU CHUA CO):
       - Hoac su dung raw query trong controller (simple hon, chi 1 field)
       - Recommendation: dung raw query vi chi 1 use case

    4. Update cancelOrder trong paperOrder.controller.js:
       - Sau khi cancel thanh cong:
       - Tinh refundAmount = order.limit_price * order.quantity + buyFee (neu da deduct)
       - Goi PaperCapitalService.refundForCancel(portfolio.id, refundAmount)
       - Chi refund neu order co simulation_mode === 'REALISTIC' (INSTANT da fill ngay, khong can refund)
       - Hoac: chi refund cho orders co status PENDING khi cancel (FILLED orders khong co gi de refund)

    5. Update orders.routes.js:
       - import { editOrder, editOrderSchema } from '../controllers/paper/paperOrder.controller.js'
       - Hoac tu order.controller.js tuy theo existing import structure
       - router.patch('/:id', validate(editOrderSchema), editOrder)

    6. Viet tests:
       - Mock Order.findById, query, PaperCapitalService
       - Test edit limit_price happy path
       - Test edit quantity happy path
       - Test edit non-PENDING order -> 409
       - Test edit voi khong du balance -> 422
       - Test cancel voi refund
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-be && npx vitest run tests/services/paperOrderManagement.test.js --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "editOrder" ai-stoploss-engine-be/controllers/paper/paperOrder.controller.js
    - grep -q "editOrderSchema" ai-stoploss-engine-be/controllers/paper/paperOrder.controller.js
    - grep -q "router.patch" ai-stoploss-engine-be/routes/orders.routes.js
    - grep -q "refundForCancel" ai-stoploss-engine-be/controllers/paper/paperOrder.controller.js
    - grep -q "ORDER_MODIFIED" ai-stoploss-engine-be/controllers/paper/paperOrder.controller.js
    - test -f ai-stoploss-engine-be/tests/services/paperOrderManagement.test.js
  </acceptance_criteria>
  <done>
    PATCH /api/portfolios/:portfolioId/orders/:id endpoint hoat dong: sua limit_price va quantity cho PENDING orders.
    limit_price auto-snap to tick size. quantity phai la boi 100.
    Virtual balance duoc adjust khi edit quantity (deduct them hoac refund).
    Cancel order refund virtual balance.
    All tests pass.
  </done>
</task>

</tasks>

<verification>
cd ai-stoploss-engine-be && npx vitest run tests/services/paperOrderManagement.test.js --reporter=verbose
</verification>

<success_criteria>
- PATCH endpoint cho edit PENDING orders (limit_price + quantity per D-09)
- Cancel flow refund virtual balance
- Tick size + price band validation cho edited price
- Lot size validation cho quantity (boi 100)
</success_criteria>

<output>
After completion, create `.planning/phases/02-paper-trading-engine/02-03-SUMMARY.md`
</output>
