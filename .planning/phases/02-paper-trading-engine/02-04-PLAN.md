---
phase: 02-paper-trading-engine
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - ai-stoploss-engine-be/services/paper/paperPerformanceService.js
  - ai-stoploss-engine-be/controllers/paper/paperPerformance.controller.js
  - ai-stoploss-engine-be/routes/orders.routes.js
  - ai-stoploss-engine-be/tests/services/paperPerformance.test.js
autonomous: true
requirements:
  - PAPER-06
must_haves:
  truths:
    - "Performance report chi tinh PAPER positions (WHERE context = 'PAPER') -- per Pitfall 6"
    - "Report hien thi: Total P&L, Win Rate, Average Win/Loss, Profit Factor, Max Drawdown (per D-11)"
    - "Buy & Hold comparison: tinh return neu user giu tat ca co phieu da mua tu dau (per D-12)"
    - "Filter theo tuan/thang/tong cong (per D-13)"
    - "Buy & Hold calculation tru SELL orders dung (per Pitfall 7)"
  artifacts:
    - path: "ai-stoploss-engine-be/services/paper/paperPerformanceService.js"
      provides: "Performance calculation + buy & hold comparison"
      exports: ["PaperPerformanceService"]
      min_lines: 100
    - path: "ai-stoploss-engine-be/controllers/paper/paperPerformance.controller.js"
      provides: "GET endpoint cho performance report"
      exports: ["getPerformanceReport", "performanceQuerySchema"]
  key_links:
    - from: "paperPerformance.controller.js"
      to: "paperPerformanceService.js"
      via: "service call for report data"
      pattern: "PaperPerformanceService"
    - from: "orders.routes.js"
      to: "paperPerformance.controller.js"
      via: "GET route for performance"
      pattern: "getPerformanceReport"
---

<objective>
Tao Paper Trading Performance Report: service tinh P&L, win rate, profit factor, max drawdown, va so sanh Buy & Hold.

Purpose: Per D-11, D-12, D-13, user can xem hieu suat paper trading cua minh. Report rieng biet cho PAPER context (per Pitfall 6), co filter theo thoi gian.

Output: PaperPerformanceService, controller, route, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-paper-trading-engine/02-CONTEXT.md
@.planning/phases/02-paper-trading-engine/02-RESEARCH.md

<interfaces>
From ai-stoploss-engine-be/config/database.js:
```javascript
export function query(sql, params) { ... }
```

From ai-stoploss-engine-be/models/Position.js (inferred from schema):
```
financial.positions: id, portfolio_id, symbol, entry_price, quantity,
  profit_loss_vnd, status ('OPEN'|'CLOSED'), context ('REAL'|'PAPER'),
  side, opened_at, closed_at, exit_price, buy_fee_vnd, sell_fee_vnd, sell_tax_vnd
```

From existing market API:
```
GET /api/market/symbols/:symbol/price?exchange=HOSE -> { data: { price } }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PaperPerformanceService voi metrics + Buy & Hold</name>
  <files>
    ai-stoploss-engine-be/services/paper/paperPerformanceService.js,
    ai-stoploss-engine-be/tests/services/paperPerformance.test.js
  </files>
  <read_first>
    ai-stoploss-engine-be/models/Position.js,
    ai-stoploss-engine-be/config/database.js,
    ai-stoploss-engine-be/tests/helpers/dbMock.js,
    ai-stoploss-engine-be/services/shared/marketPriceService.js
  </read_first>
  <behavior>
    - getPerformanceReport voi 3 closed positions (2 win, 1 loss):
      * total_trades = 3, winning_trades = 2, losing_trades = 1
      * win_rate = 66.67%
      * total_pnl = sum of all profit_loss_vnd
      * avg_win = average of positive P&L
      * avg_loss = average of negative P&L (absolute)
      * profit_factor = gross_profit / gross_loss
    - getPerformanceReport voi 0 trades: return zeroed metrics
    - max_drawdown: tinh tu equity curve cua closed positions theo thoi gian
    - getBuyAndHoldReturn: tinh dung khi co ca BUY va SELL positions
    - getBuyAndHoldReturn: chi tinh net holdings (tru da ban) -- per Pitfall 7
    - Time filter: period = 'week' chi tinh trades closed trong 7 ngay qua
    - Time filter: period = 'month' chi tinh trades closed trong 30 ngay qua
  </behavior>
  <action>
    1. Tao PaperPerformanceService class:

       - static async getPerformanceReport(portfolioId, { period = 'all' } = {}):
         * Build date filter: 'week' -> 7 days, 'month' -> 30 days, 'all' -> no filter
         * Query closed PAPER positions:
           ```sql
           SELECT COUNT(*) FILTER (WHERE status != 'OPEN') AS total_trades,
             COUNT(*) FILTER (WHERE profit_loss_vnd > 0 AND status != 'OPEN') AS winning_trades,
             COUNT(*) FILTER (WHERE profit_loss_vnd < 0 AND status != 'OPEN') AS losing_trades,
             COALESCE(SUM(profit_loss_vnd) FILTER (WHERE status != 'OPEN'), 0) AS total_pnl,
             COALESCE(SUM(profit_loss_vnd) FILTER (WHERE profit_loss_vnd > 0 AND status != 'OPEN'), 0) AS gross_profit,
             COALESCE(ABS(SUM(profit_loss_vnd) FILTER (WHERE profit_loss_vnd < 0 AND status != 'OPEN')), 0) AS gross_loss,
             COALESCE(AVG(profit_loss_vnd) FILTER (WHERE profit_loss_vnd > 0 AND status != 'OPEN'), 0) AS avg_win,
             COALESCE(ABS(AVG(profit_loss_vnd) FILTER (WHERE profit_loss_vnd < 0 AND status != 'OPEN')), 0) AS avg_loss
           FROM financial.positions
           WHERE portfolio_id = $1 AND context = 'PAPER'
             AND (closed_at >= $2 OR $2 IS NULL)
           ```
         * Tinh derived metrics:
           - win_rate = winning_trades / total_trades * 100
           - profit_factor = gross_profit / max(gross_loss, 1) (avoid div by 0)

       - static async getMaxDrawdown(portfolioId, { period = 'all' } = {}):
         * Query closed positions ORDER BY closed_at ASC
         * Build equity curve: running sum of profit_loss_vnd
         * Calculate max drawdown: max(peak - trough) / peak * 100
         * Return { max_drawdown_vnd, max_drawdown_pct }

       - static async getBuyAndHoldReturn(portfolioId):
         * Per Pitfall 7: tinh NET holdings, khong chi BUY
         * Query: SELECT symbol, SUM(CASE WHEN side='LONG' THEN quantity ELSE -quantity END) AS net_qty,
                  SUM(CASE WHEN side='LONG' THEN entry_price * quantity ELSE 0 END) AS total_buy_cost
                  FROM financial.positions WHERE portfolio_id = $1 AND context = 'PAPER' GROUP BY symbol HAVING net_qty > 0
         * For each symbol voi net_qty > 0: fetch current market price
         * buy_hold_value = SUM(current_price * net_qty)
         * buy_hold_cost = SUM(total_buy_cost tuy chinh theo net_qty)
         * buy_hold_return = buy_hold_value - buy_hold_cost
         * buy_hold_return_pct = buy_hold_return / buy_hold_cost * 100

       - static async getFullReport(portfolioId, options):
         * Combine getPerformanceReport + getMaxDrawdown + getBuyAndHoldReturn
         * Return full report object

    2. Viet tests (TDD):
       - Mock database query
       - Test voi 3 closed positions (2 win, 1 loss): verify win_rate, profit_factor
       - Test voi 0 trades: zeroed metrics
       - Test max drawdown calculation voi known equity curve
       - Test buy & hold voi net holdings (per Pitfall 7)
       - Test time filter period = 'week' va 'month'
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-be && npx vitest run tests/services/paperPerformance.test.js --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "getPerformanceReport" ai-stoploss-engine-be/services/paper/paperPerformanceService.js
    - grep -q "getMaxDrawdown" ai-stoploss-engine-be/services/paper/paperPerformanceService.js
    - grep -q "getBuyAndHoldReturn" ai-stoploss-engine-be/services/paper/paperPerformanceService.js
    - grep -q "context = 'PAPER'" ai-stoploss-engine-be/services/paper/paperPerformanceService.js
    - grep -q "profit_factor" ai-stoploss-engine-be/services/paper/paperPerformanceService.js
    - grep -q "win_rate" ai-stoploss-engine-be/services/paper/paperPerformanceService.js
    - test -f ai-stoploss-engine-be/tests/services/paperPerformance.test.js
  </acceptance_criteria>
  <done>
    PaperPerformanceService tinh day du: total P&L, win rate, avg win/loss, profit factor, max drawdown (D-11).
    Buy & Hold comparison tinh dung net holdings (D-12, Pitfall 7).
    Filter theo week/month/all (D-13).
    Chi query PAPER positions (Pitfall 6).
  </done>
</task>

<task type="auto">
  <name>Task 2: Performance report API endpoint + route</name>
  <files>
    ai-stoploss-engine-be/controllers/paper/paperPerformance.controller.js,
    ai-stoploss-engine-be/routes/orders.routes.js
  </files>
  <read_first>
    ai-stoploss-engine-be/services/paper/paperPerformanceService.js,
    ai-stoploss-engine-be/controllers/paper/paperOrder.controller.js,
    ai-stoploss-engine-be/routes/orders.routes.js,
    ai-stoploss-engine-be/routes/portfolio.routes.js,
    ai-stoploss-engine-be/middleware/validation.js
  </read_first>
  <action>
    1. Tao paperPerformance.controller.js:
       - import PaperPerformanceService
       - import Joi, Portfolio

       - export const performanceQuerySchema = Joi.object({
           period: Joi.string().valid('all', 'week', 'month').default('all'),
         })

       - export const getPerformanceReport = async (req, res, next):
         * ensurePortfolioOwnership (copy pattern tu paperOrder.controller.js)
         * const { period } = req.query
         * const report = await PaperPerformanceService.getFullReport(portfolioId, { period })
         * res.json({ success: true, data: report })

    2. Them route vao orders.routes.js (hoac portfolio.routes.js):
       - Xem xau existing route structure de quyet dinh dang route tot nhat
       - Option A: GET /api/portfolios/:portfolioId/paper-performance?period=all
       - Option B: GET /api/portfolios/:portfolioId/orders/performance?period=all
       - Chon option phu hop nhat voi existing route patterns
       - import { getPerformanceReport, performanceQuerySchema } from controllers
       - router.get('/performance' hoac '/paper-performance', validateQuery(performanceQuerySchema), getPerformanceReport)

    3. Dang ky route trong routes/index.js neu can (check existing pattern).
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-be && grep -q "getPerformanceReport" controllers/paper/paperPerformance.controller.js && grep -rq "performance" routes/ && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "getPerformanceReport" ai-stoploss-engine-be/controllers/paper/paperPerformance.controller.js
    - grep -q "PaperPerformanceService" ai-stoploss-engine-be/controllers/paper/paperPerformance.controller.js
    - grep -q "performanceQuerySchema" ai-stoploss-engine-be/controllers/paper/paperPerformance.controller.js
    - grep -rq "performance" ai-stoploss-engine-be/routes/
    - test -f ai-stoploss-engine-be/controllers/paper/paperPerformance.controller.js
  </acceptance_criteria>
  <done>
    GET endpoint tra ve performance report voi period filter.
    Route registered va accessible.
    Controller dung PaperPerformanceService.getFullReport.
  </done>
</task>

</tasks>

<verification>
cd ai-stoploss-engine-be && npx vitest run tests/services/paperPerformance.test.js --reporter=verbose && grep -rq "performance" routes/
</verification>

<success_criteria>
- Performance report API: Total P&L, Win Rate, Avg Win/Loss, Profit Factor, Max Drawdown (D-11)
- Buy & Hold comparison voi net holdings (D-12, Pitfall 7)
- Period filter: all/week/month (D-13)
- Chi PAPER positions (Pitfall 6)
</success_criteria>

<output>
After completion, create `.planning/phases/02-paper-trading-engine/02-04-SUMMARY.md`
</output>
