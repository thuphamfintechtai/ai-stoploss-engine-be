---
phase: 01-foundation-portfolio-management
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-stoploss-engine-be/tests/services/realOrderService.test.js
autonomous: true
gap_closure: true
requirements: [FOUND-03, PORT-03]

must_haves:
  truths:
    - "35 tests dang pass van tiep tuc pass sau khi fix"
    - "4 tests trong realOrderService.test.js FAIL truoc day nay pass"
    - "Toan bo 39 tests pass khi chay npx vitest run"
  artifacts:
    - path: "ai-stoploss-engine-be/tests/services/realOrderService.test.js"
      provides: "Test suite da mock CapitalService.deductForBuy"
      contains: "vi.mock.*capitalService"
  key_links:
    - from: "realOrderService.test.js"
      to: "services/portfolio/capitalService.js"
      via: "vi.mock('./capitalService.js')"
      pattern: "vi\\.mock.*capitalService"
---

<objective>
Fix test regression: 4 tests trong realOrderService.test.js FAIL vi Plan 06 wire CapitalService.deductForBuy vao realOrderService nhung khong update mock tuong ung trong test file.

Purpose: Phase 01 khong the duoc danh dau complete khi co failing tests. Day la regression do Plan 06 bo sung CapitalService integration ma khong cap nhat test suite.

Output: realOrderService.test.js voi mock CapitalService day du, 39/39 tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@ai-stoploss-engine-be/tests/services/realOrderService.test.js
@ai-stoploss-engine-be/services/portfolio/realOrderService.js
@ai-stoploss-engine-be/services/portfolio/capitalService.js
</context>

<interfaces>
<!-- Root cause: realOrderService.js line 13 va 49 -->
<!-- import CapitalService from './capitalService.js'; -->
<!-- await CapitalService.deductForBuy(portfolioId, totalCost); -->
<!--
  Tests dang dung vi.mock database.js tra ve rows=[] trong transaction callback.
  Khi CapitalService.deductForBuy chay voi mock client, no goi SELECT FOR UPDATE
  -> rows[0].available_cash = undefined -> NaN < totalCost -> throw 422.

  Fix: Them vi.mock('./capitalService.js') truoc import RealOrderService.
-->

<!-- CapitalService interface can mock -->
From ai-stoploss-engine-be/services/portfolio/capitalService.js:
```javascript
const CapitalService = {
  deductForBuy: async (portfolioId, amount) => { /* throws 422 if insufficient */ },
  addPendingSettlement: async (portfolioId, amount, settlementDate) => {},
  getBalance: async (portfolioId) => { /* returns { total_balance, available_cash, pending_settlement_cash } */ },
};
export default CapitalService;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Them vi.mock CapitalService vao realOrderService.test.js</name>
  <files>ai-stoploss-engine-be/tests/services/realOrderService.test.js</files>
  <read_first>
    - ai-stoploss-engine-be/tests/services/realOrderService.test.js — doc toan bo de hieu cau truc mock hien tai
    - ai-stoploss-engine-be/services/portfolio/realOrderService.js — xac nhan duong dan import capitalService
  </read_first>
  <action>
    Them vi.mock cho CapitalService TRUOC DONG `import RealOrderService`. Vi tri: sau vi.mock('../../services/shared/feeEngine.js') va truoc cac dong import.

    Them vao cuoi phan vi.mock block (truoc dong `import { query } from '../../config/database.js'`):

    ```javascript
    vi.mock('../../services/portfolio/capitalService.js', () => ({
      default: {
        deductForBuy: vi.fn().mockResolvedValue(undefined),
        addPendingSettlement: vi.fn().mockResolvedValue(undefined),
        getBalance: vi.fn().mockResolvedValue({
          total_balance: 10000000000,
          available_cash: 10000000000,
          pending_settlement_cash: 0,
        }),
      },
    }));
    ```

    Duong dan mock la `'../../services/portfolio/capitalService.js'` -- phai match chinh xac voi duong dan import trong realOrderService.js (line 13: `import CapitalService from './capitalService.js'`).

    KHONG thay doi bat ky test case nao. Chi them vi.mock block.
  </action>
  <verify>
    <automated>cd /Users/phamthianhthu/ai-stoploss-engine/ai-stoploss-engine-web/ai-stoploss-engine-be && npx vitest run tests/services/realOrderService.test.js --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 4 tests "recordBuyOrder" da PASS (khong con throw 422)
    - Khong co test nao khac bi anh huong (regression-free)
    - Output cuoi: "4 passed" hoac "Tests 4 passed"
  </done>
  <acceptance_criteria>
    - vi.mock capitalService ton tai trong file test
    - `npx vitest run` bao cao 0 failures
    - 4 recordBuyOrder tests pass, 2 getTransactionHistory tests pass = 6 tests total pass
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify toan bo test suite (39 tests)</name>
  <files></files>
  <read_first>
    - Khong can doc file -- chi chay lenh kiem tra
  </read_first>
  <action>
    Chay full test suite de xac nhan fix khong break bat ky test nao khac.

    Neu co test nao bat ngo fail (khong phai realOrderService), dieu tra nguyen nhan va fix trong pham vi test file do (khong thay doi implementation code).
  </action>
  <verify>
    <automated>cd /Users/phamthianhthu/ai-stoploss-engine/ai-stoploss-engine-web/ai-stoploss-engine-be && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Output bao cao "39 passed" hoac "Tests X passed" (khong co failed)
    - Khong co dong "failed" trong output
  </done>
  <acceptance_criteria>
    - Toan bo test suite pass: 0 failures
    - realOrderService tests: 6/6 pass (4 recordBuyOrder + 2 getTransactionHistory)
    - Khong co test khac bi break
  </acceptance_criteria>
</task>

</tasks>

<verification>
cd /Users/phamthianhthu/ai-stoploss-engine/ai-stoploss-engine-web/ai-stoploss-engine-be && npx vitest run 2>&1 | grep -E "passed|failed|Tests"
</verification>

<success_criteria>
- 39/39 tests pass (khong co failure nao)
- vi.mock('../../services/portfolio/capitalService.js') duoc them vao realOrderService.test.js
- realOrderService.test.js chi thay doi o phan mock setup (khong thay doi test logic)
- Gap 2 tu VERIFICATION.md duoc dong: "Unit tests pass cho RealOrderService"
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-portfolio-management/01-07-SUMMARY.md` with:
- Files modified
- What was fixed (root cause + solution)
- Test results (X passed, 0 failed)
</output>
