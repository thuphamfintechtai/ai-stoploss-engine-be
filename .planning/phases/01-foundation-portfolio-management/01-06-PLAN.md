---
phase: 01-foundation-portfolio-management
plan: 06
type: execute
wave: 3
depends_on: [01-02, 01-03, 01-04]
files_modified:
  - ai-stoploss-engine-be/services/portfolio/realOrderService.js
  - ai-stoploss-engine-be/services/portfolio/realPositionService.js
  - ai-stoploss-engine-be/controllers/portfolio/realOrder.controller.js
  - ai-stoploss-engine-be/controllers/portfolio/realPosition.controller.js
  - ai-stoploss-engine-be/controllers/portfolio/portfolioSummary.controller.js
  - ai-stoploss-engine-be/routes/portfolio.routes.js
  - ai-stoploss-engine-be/tests/services/portfolioSummary.test.js
autonomous: true
requirements:
  - PORT-06
  - PORT-03

must_haves:
  truths:
    - "User co the xem tong quan portfolio (tong gia tri, tong P&L, % return)"
    - "Real order buy flow tru available_cash qua CapitalService"
    - "Real position close flow them pending_settlement qua CapitalService"
    - "Portfolio summary endpoint tra ve tong gia tri, total P&L, % return"
  artifacts:
    - path: "ai-stoploss-engine-be/controllers/portfolio/portfolioSummary.controller.js"
      provides: "Portfolio summary endpoint"
      exports: ["getPortfolioSummary"]
      contains: "total_pnl"
    - path: "ai-stoploss-engine-be/tests/services/portfolioSummary.test.js"
      provides: "Tests cho portfolio summary"
      contains: "total_value"
  key_links:
    - from: "controllers/portfolio/realOrder.controller.js"
      to: "services/portfolio/capitalService.js"
      via: "deductForBuy on BUY order"
      pattern: "CapitalService.deductForBuy"
    - from: "controllers/portfolio/realPosition.controller.js"
      to: "services/portfolio/capitalService.js"
      via: "addPendingSettlement on position close"
      pattern: "CapitalService.addPendingSettlement"
    - from: "routes/portfolio.routes.js"
      to: "controllers/portfolio/portfolioSummary.controller.js"
      via: "GET summary endpoint"
      pattern: "real-summary"
---

<objective>
Wire CapitalService vao real order/position flow (buy tru tien, sell them pending settlement) va xay dung portfolio summary endpoint (tong gia tri, tong P&L, % return).

Purpose: Hoan thien cash flow logic va cung cap endpoint cho portfolio overview. Day la plan cuoi cua backend.

Output: Updated order/position services voi cash flow, portfolio summary endpoint + tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-portfolio-management/01-CONTEXT.md
@.planning/phases/01-foundation-portfolio-management/01-RESEARCH.md
@.planning/phases/01-foundation-portfolio-management/01-02-SUMMARY.md
@.planning/phases/01-foundation-portfolio-management/01-03-SUMMARY.md

<interfaces>
From ai-stoploss-engine-be/services/portfolio/capitalService.js (Plan 03):
```javascript
class CapitalService {
  static async deductForBuy(portfolioId, totalCost) { ... }
  // Throws { statusCode: 422, message: 'Khong du tien mat kha dung' } khi khong du
  static async addPendingSettlement(portfolioId, netAmount, settlementDate) { ... }
  static async getBalance(portfolioId) { ... }
  // Returns: { total_balance, available_cash, pending_settlement_cash, deployed_cash }
}
export function addBusinessDays(date, days) { ... }
export default CapitalService;
```

From ai-stoploss-engine-be/services/portfolio/realOrderService.js (Plan 02):
```javascript
class RealOrderService {
  static async recordBuyOrder(portfolioId, { symbol, exchange, quantity, filledPrice, filledDate }) { ... }
  static async getTransactionHistory(portfolioId, { page, limit }) { ... }
}
export default RealOrderService;
```

From ai-stoploss-engine-be/services/portfolio/realPositionService.js (Plan 02):
```javascript
class RealPositionService {
  static async closePosition(positionId, { sellPrice, sellDate, portfolioId }) { ... }
  static async getOpenPositions(portfolioId) { ... }
}
export default RealPositionService;
```

From ai-stoploss-engine-be/services/shared/feeEngine.js:
```javascript
export function calculateFees(entryVnd, closeVnd, qty, portfolio = {}) { ... }
export function calculateBuyFee(entryVnd, qty, portfolio = {}) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire CapitalService vao Real Order + Position Close Flow</name>
  <files>
    ai-stoploss-engine-be/services/portfolio/realOrderService.js,
    ai-stoploss-engine-be/services/portfolio/realPositionService.js,
    ai-stoploss-engine-be/controllers/portfolio/realOrder.controller.js,
    ai-stoploss-engine-be/controllers/portfolio/realPosition.controller.js
  </files>
  <read_first>
    - ai-stoploss-engine-be/services/portfolio/realOrderService.js
    - ai-stoploss-engine-be/services/portfolio/realPositionService.js
    - ai-stoploss-engine-be/services/portfolio/capitalService.js
    - ai-stoploss-engine-be/controllers/portfolio/realOrder.controller.js
    - ai-stoploss-engine-be/controllers/portfolio/realPosition.controller.js
    - ai-stoploss-engine-be/services/shared/feeEngine.js
  </read_first>
  <action>
    Per D-08, D-09:

    **Update realOrderService.js** -- recordBuyOrder:
    1. Import CapitalService: `import CapitalService from './capitalService.js';`
    2. Import calculateBuyFee: `import { calculateBuyFee } from '../shared/feeEngine.js';`
    3. Truoc khi tao order, tinh totalCost:
       ```javascript
       const buyFee = calculateBuyFee(filledPrice, quantity);
       const totalCost = (filledPrice * quantity) + buyFee;
       ```
    4. Goi CapitalService.deductForBuy(portfolioId, totalCost) TRUOC khi tao order/position
    5. Neu deductForBuy throw (khong du tien) -> error bubble up tu nhien

    **Update realPositionService.js** -- closePosition:
    1. Import CapitalService va addBusinessDays:
       ```javascript
       import CapitalService, { addBusinessDays } from './capitalService.js';
       ```
    2. Sau khi tinh fees va update position, them:
       ```javascript
       // Tien ban (sau phi + thue) vao pending settlement
       const netSellProceeds = sellPrice * quantity - fees.sell_fee_vnd - fees.sell_tax_vnd;
       const settlementDate = addBusinessDays(sellDate, 2);
       await CapitalService.addPendingSettlement(portfolioId, netSellProceeds, settlementDate);
       ```
    3. Return them settlementDate trong response

    **Update controllers** -- them try/catch cu the cho 422 errors:
    Trong realOrder.controller.js createRealOrder:
    ```javascript
    // Catch CapitalService error:
    if (error.statusCode === 422) {
      return res.status(422).json({ success: false, message: error.message });
    }
    ```
  </action>
  <verify>
    <automated>grep -q "CapitalService.deductForBuy" ai-stoploss-engine-be/services/portfolio/realOrderService.js && grep -q "CapitalService.addPendingSettlement" ai-stoploss-engine-be/services/portfolio/realPositionService.js && grep -q "addBusinessDays" ai-stoploss-engine-be/services/portfolio/realPositionService.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - realOrderService.js import va goi CapitalService.deductForBuy truoc khi tao order
    - realOrderService.js tinh totalCost = (filledPrice * quantity) + buyFee
    - realPositionService.js import va goi CapitalService.addPendingSettlement
    - realPositionService.js tinh settlementDate = addBusinessDays(sellDate, 2)
    - realPositionService.js tinh netSellProceeds = sellValue - sell_fee - sell_tax
    - Controller handle 422 error response
  </acceptance_criteria>
  <done>CapitalService wired vao BUY (deduct cash) va SELL (pending settlement voi T+2) flows</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Portfolio Summary Endpoint (tong gia tri, P&L, % return)</name>
  <files>
    ai-stoploss-engine-be/controllers/portfolio/portfolioSummary.controller.js,
    ai-stoploss-engine-be/routes/portfolio.routes.js,
    ai-stoploss-engine-be/tests/services/portfolioSummary.test.js
  </files>
  <read_first>
    - ai-stoploss-engine-be/controllers/portfolio.controller.js
    - ai-stoploss-engine-be/routes/portfolio.routes.js
    - ai-stoploss-engine-be/services/portfolio/capitalService.js
    - ai-stoploss-engine-be/models/Portfolio.js
    - ai-stoploss-engine-be/models/Position.js
    - ai-stoploss-engine-be/tests/helpers/db.js
  </read_first>
  <behavior>
    getPortfolioSummary(portfolioId):
    - Tinh total_value = sum(position.entry_price * position.quantity) cho OPEN real positions
    - Tinh total_realized_pnl = sum(position.realized_pnl) cho CLOSED_MANUAL real positions
    - Tinh total_unrealized_pnl = sum((currentPrice - entryPrice) * qty) cho OPEN real positions (currentPrice tu market data hoac entry_price neu khong co)
    - Tinh percent_return = (total_realized_pnl + total_unrealized_pnl) / total_balance * 100
    - Tra ve: { total_value, total_realized_pnl, total_unrealized_pnl, total_pnl, percent_return, position_count, cash_balance }
  </behavior>
  <action>
    **portfolioSummary.controller.js** (`controllers/portfolio/portfolioSummary.controller.js`):
    ```javascript
    import { query } from '../../config/database.js';
    import CapitalService from '../../services/portfolio/capitalService.js';

    export const getPortfolioSummary = async (req, res, next) => {
      try {
        const portfolioId = req.params.portfolioId || req.params.id;
        // ensurePortfolioOwnership pattern

        // 1. Get cash balance
        const balance = await CapitalService.getBalance(portfolioId);

        // 2. Get OPEN real positions aggregate
        const { rows: openAgg } = await query(
          `SELECT
            COUNT(*) as position_count,
            COALESCE(SUM(entry_price * quantity), 0) as total_invested,
            COALESCE(SUM(quantity), 0) as total_shares
          FROM financial.positions
          WHERE portfolio_id = $1 AND context = 'REAL' AND status = 'OPEN'`,
          [portfolioId]
        );

        // 3. Get realized P&L from CLOSED positions
        const { rows: closedAgg } = await query(
          `SELECT
            COUNT(*) as closed_count,
            COALESCE(SUM(realized_pnl_vnd), 0) as total_realized_pnl
          FROM financial.positions
          WHERE portfolio_id = $1 AND context = 'REAL' AND status = 'CLOSED_MANUAL'`,
          [portfolioId]
        );

        // Note: unrealized P&L can current market price -- tam thoi dung entry_price
        // Phase 5 se them market price integration
        const totalValue = parseFloat(openAgg[0]?.total_invested || 0);
        const totalRealizedPnl = parseFloat(closedAgg[0]?.total_realized_pnl || 0);
        const totalUnrealizedPnl = 0; // TODO: integrate market price in later phase
        const totalPnl = totalRealizedPnl + totalUnrealizedPnl;
        const percentReturn = balance.total_balance > 0
          ? (totalPnl / balance.total_balance * 100)
          : 0;

        return res.json({
          success: true,
          data: {
            total_value: totalValue,
            total_realized_pnl: totalRealizedPnl,
            total_unrealized_pnl: totalUnrealizedPnl,
            total_pnl: totalPnl,
            percent_return: Math.round(percentReturn * 100) / 100,
            position_count: parseInt(openAgg[0]?.position_count || 0),
            closed_count: parseInt(closedAgg[0]?.closed_count || 0),
            cash_balance: balance,
          },
        });
      } catch (error) {
        next(error);
      }
    };
    ```

    **Update routes/portfolio.routes.js** -- them route:
    ```javascript
    import * as portfolioSummaryController from '../controllers/portfolio/portfolioSummary.controller.js';
    // Them route:
    router.get('/:portfolioId/real-summary', portfolioSummaryController.getPortfolioSummary);
    ```

    **Test** (`tests/services/portfolioSummary.test.js`):
    - Mock db queries
    - Test tong gia tri correct khi co 2 OPEN positions
    - Test realized P&L correct khi co 1 CLOSED_MANUAL position
    - Test percent_return calculation
    - Test empty portfolio returns zeros
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-be && npx vitest run tests/services/portfolioSummary.test.js --reporter=verbose 2>&1 | tail -15 && grep -q "real-summary" routes/portfolio.routes.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File controllers/portfolio/portfolioSummary.controller.js ton tai
    - Chua query voi "context = 'REAL'" va "status = 'OPEN'"
    - Chua query voi "status = 'CLOSED_MANUAL'" cho realized P&L
    - Export getPortfolioSummary
    - routes/portfolio.routes.js co route GET /:portfolioId/real-summary
    - Tests pass voi 4+ test cases
    - Response structure co: total_value, total_realized_pnl, total_pnl, percent_return, cash_balance
  </acceptance_criteria>
  <done>Portfolio summary endpoint hoan chinh voi total value, P&L, % return, va cash balance</done>
</task>

</tasks>

<verification>
- CapitalService wired into buy (deduct) va sell (pending settlement) flows
- Portfolio summary endpoint returns correct data
- Tests pass
- T+2 settlement date tinh dung
</verification>

<success_criteria>
1. Buy order tru available_cash (totalCost + fee)
2. Position close them pending_settlement voi T+2 settlement date
3. Portfolio summary endpoint tra ve tong gia tri, realized/unrealized P&L, % return
4. All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-portfolio-management/01-06-SUMMARY.md`
</output>
