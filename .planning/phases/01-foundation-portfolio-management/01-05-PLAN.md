---
phase: 01-foundation-portfolio-management
plan: 05
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - ai-stoploss-engine-fe/components/portfolio/RealOrderForm.tsx
  - ai-stoploss-engine-fe/components/portfolio/CashBalanceCard.tsx
  - ai-stoploss-engine-fe/components/portfolio/RealPositionsTable.tsx
  - ai-stoploss-engine-fe/components/portfolio/TransactionHistory.tsx
  - ai-stoploss-engine-fe/components/portfolio/ClosePositionModal.tsx
  - ai-stoploss-engine-fe/components/PortfolioView.tsx
  - ai-stoploss-engine-fe/services/api.ts
autonomous: false
requirements:
  - PORT-01
  - PORT-02
  - PORT-04
  - PORT-05
  - PORT-06

must_haves:
  truths:
    - "User co the nhap lenh that qua form don gian (ma CK, exchange, side, so luong, gia khop, ngay)"
    - "User co the xem cash balance (available, pending, deployed)"
    - "User co the xem danh sach positions REAL dang mo"
    - "User co the dong vi the qua modal (nhap gia ban + ngay ban)"
    - "User co the xem lich su giao dich"
    - "User co the xem tong quan portfolio (tong P&L, % return)"
    - "PortfolioView co tab Real | Paper de chuyen doi"
  artifacts:
    - path: "ai-stoploss-engine-fe/components/portfolio/RealOrderForm.tsx"
      provides: "Form nhap lenh that"
      contains: "RealOrderForm"
      min_lines: 80
    - path: "ai-stoploss-engine-fe/components/portfolio/CashBalanceCard.tsx"
      provides: "Card hien thi cash balance"
      contains: "available_cash"
    - path: "ai-stoploss-engine-fe/components/portfolio/RealPositionsTable.tsx"
      provides: "Bang vi the REAL dang mo"
      contains: "RealPositionsTable"
    - path: "ai-stoploss-engine-fe/components/portfolio/ClosePositionModal.tsx"
      provides: "Modal dong vi the"
      contains: "sell_price"
    - path: "ai-stoploss-engine-fe/components/portfolio/TransactionHistory.tsx"
      provides: "Lich su giao dich"
      contains: "TransactionHistory"
    - path: "ai-stoploss-engine-fe/components/PortfolioView.tsx"
      provides: "Updated view voi Real/Paper tab"
      contains: "activeTab"
  key_links:
    - from: "components/portfolio/RealOrderForm.tsx"
      to: "/api/portfolios/:id/real-orders"
      via: "POST request"
      pattern: "real-orders"
    - from: "components/portfolio/ClosePositionModal.tsx"
      to: "/api/portfolios/:id/real-positions/:positionId/close"
      via: "POST request"
      pattern: "real-positions.*close"
    - from: "components/PortfolioView.tsx"
      to: "components/portfolio/RealOrderForm.tsx"
      via: "import and render in Real tab"
      pattern: "import.*RealOrderForm"
    - from: "components/portfolio/CashBalanceCard.tsx"
      to: "GET /api/portfolios/:id"
      via: "PortfolioView fetches portfolio data including available_cash and pending_settlement_cash (added by migration 007 in Plan 01)"
      pattern: "available_cash"
---

<objective>
Xay dung frontend components cho Portfolio Management: form nhap lenh that, cash balance card, danh sach positions, modal dong vi the, lich su giao dich, va tong quan portfolio. Them tab Real/Paper vao PortfolioView.

Purpose: Day la UX layer cho user tuong tac voi real order flow. Form phai don gian (per D-04), phi tu dong tinh (per D-05).

Output: 6 React components + updated PortfolioView + API service updates
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-portfolio-management/01-CONTEXT.md
@.planning/phases/01-foundation-portfolio-management/01-RESEARCH.md
@.planning/phases/01-foundation-portfolio-management/01-02-SUMMARY.md
@.planning/phases/01-foundation-portfolio-management/01-03-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

<interfaces>
<!-- Backend API endpoints (from Plan 02) -->
POST /api/portfolios/:portfolioId/real-orders
  Body: { symbol, exchange, side, quantity, filled_price, filled_date, notes? }
  Response: { success: true, data: { order, position } }

GET /api/portfolios/:portfolioId/real-orders?page=1&limit=50
  Response: { success: true, data: { orders, total, page, limit } }

GET /api/portfolios/:portfolioId/real-positions
  Response: { success: true, data: positions[] }

POST /api/portfolios/:portfolioId/real-positions/:positionId/close
  Body: { sell_price, sell_date, notes? }
  Response: { success: true, data: { position, pnl } }

<!-- Cash Balance: GET /api/portfolios/:id response MUST include available_cash va pending_settlement_cash -->
<!-- Day la fields duoc them boi migration 007 (Plan 01). Existing GET /api/portfolios/:id -->
<!-- endpoint da tra ve tat ca columns cua portfolios table, nen sau migration 007, -->
<!-- response se tu dong co available_cash va pending_settlement_cash. -->
<!-- VERIFY: Truoc khi implement CashBalanceCard, confirm GET /api/portfolios/:id -->
<!-- tra ve available_cash va pending_settlement_cash trong response. -->
GET /api/portfolios/:id
  Response includes: total_balance, available_cash, pending_settlement_cash

<!-- Frontend patterns -->
From ai-stoploss-engine-fe/services/api.ts:
```typescript
// Uses axios instance with baseURL and auth interceptor
// Pattern: export const portfolioApi = { getAll, getById, create, ... }
```

From ai-stoploss-engine-fe/components/PortfolioView.tsx:
```typescript
// Large component with internal state
// Fetches portfolio data via API on mount
// Uses Tailwind CSS for styling
```

From ai-stoploss-engine-fe/components/TradingTerminal.tsx:
```typescript
// Complex order form for paper trading
// Has Joi-like validation, exchange select, order type select
// Reference for form patterns
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API Service + Core Components (RealOrderForm, CashBalanceCard, RealPositionsTable)</name>
  <files>
    ai-stoploss-engine-fe/services/api.ts,
    ai-stoploss-engine-fe/components/portfolio/RealOrderForm.tsx,
    ai-stoploss-engine-fe/components/portfolio/CashBalanceCard.tsx,
    ai-stoploss-engine-fe/components/portfolio/RealPositionsTable.tsx
  </files>
  <read_first>
    - ai-stoploss-engine-fe/services/api.ts
    - ai-stoploss-engine-fe/components/TradingTerminal.tsx
    - ai-stoploss-engine-fe/components/PortfolioView.tsx
    - ai-stoploss-engine-fe/components/TraderCard.tsx
  </read_first>
  <action>
    **0. VERIFY portfolio endpoint tra ve available_cash:**
    Truoc khi code CashBalanceCard, doc `ai-stoploss-engine-be/controllers/portfolio.controller.js` va `ai-stoploss-engine-be/models/Portfolio.js` de xac nhan GET /api/portfolios/:id tra ve available_cash va pending_settlement_cash (fields duoc them boi migration 007 trong Plan 01). Neu model dung SELECT * hoac SELECT tat ca columns, thi ok -- fields moi se tu dong co trong response. Neu model chi SELECT specific columns, can verify available_cash va pending_settlement_cash co trong SELECT list. Neu thieu, them vao Portfolio model.

    **1. Update api.ts** -- them realPortfolioApi:
    ```typescript
    export const realPortfolioApi = {
      createOrder: (portfolioId: string, data: {
        symbol: string; exchange: string; side: 'BUY' | 'SELL';
        quantity: number; filled_price: number; filled_date: string; notes?: string;
      }) => api.post(`/portfolios/${portfolioId}/real-orders`, data),

      getTransactionHistory: (portfolioId: string, page = 1, limit = 50) =>
        api.get(`/portfolios/${portfolioId}/real-orders`, { params: { page, limit } }),

      getOpenPositions: (portfolioId: string) =>
        api.get(`/portfolios/${portfolioId}/real-positions`),

      closePosition: (portfolioId: string, positionId: string, data: {
        sell_price: number; sell_date: string; notes?: string;
      }) => api.post(`/portfolios/${portfolioId}/real-positions/${positionId}/close`, data),
    };
    ```

    **2. RealOrderForm.tsx** (`components/portfolio/RealOrderForm.tsx`):
    Per D-04, D-05:
    ```typescript
    interface RealOrderFormProps {
      portfolioId: string;
      availableCash: number;
      onSuccess: () => void;
    }

    export const RealOrderForm: React.FC<RealOrderFormProps> = ({ portfolioId, availableCash, onSuccess }) => {
      // State: symbol, exchange (HOSE/HNX/UPCOM), side (BUY/SELL toggle), quantity, filledPrice, filledDate (default today)
      // Auto-calculate: totalValue = quantity * filledPrice
      // Auto-calculate: fee = totalValue * 0.0015 (buy) hoac totalValue * 0.0025 (sell = 0.15% + 0.1% tax)
      // Show: "Con lai sau giao dich: availableCash - totalValue - fee" (chi khi BUY)
      // Validation: quantity > 0, filledPrice > 0, symbol required, exchange required
      // Submit: POST /real-orders -> onSuccess()
      // Tailwind: form card voi shadow, input fields voi label, submit button
      // Error display: server errors hien duoi form
    };
    ```
    Form fields (per D-04):
    - Ma CK: text input uppercase (onChange auto-uppercase)
    - San: select dropdown (HOSE, HNX, UPCOM)
    - Loai lenh: toggle MUA/BAN (2 buttons, active state voi bg-green-600/bg-red-600)
    - So luong: number input (min=100, step=100 cho lot size)
    - Gia khop: number input (VND, use toLocaleString for display)
    - Ngay khop: date input (default today, max=today)
    - Auto-display: Tong gia tri, Phi, Con lai sau GD
    - Button: "Ghi nhan lenh" (green cho BUY, red cho SELL)

    **3. CashBalanceCard.tsx** (`components/portfolio/CashBalanceCard.tsx`):
    ```typescript
    interface CashBalanceCardProps {
      totalBalance: number;
      availableCash: number;
      pendingSettlement: number;
    }

    export const CashBalanceCard: React.FC<CashBalanceCardProps> = (props) => {
      // deployedCash = totalBalance - availableCash - pendingSettlement
      // Display 4 metrics in card grid:
      //   Tong von: totalBalance (VND format)
      //   Kha dung: availableCash (green)
      //   Cho thanh toan T+2: pendingSettlement (yellow/amber)
      //   Da deploy: deployedCash (blue)
      // Tailwind: card voi grid-cols-2, each metric voi label + value
    };
    ```

    **4. RealPositionsTable.tsx** (`components/portfolio/RealPositionsTable.tsx`):
    ```typescript
    interface RealPosition {
      id: string; symbol: string; exchange: string;
      entry_price: number; quantity: number;
      status: string; created_at: string;
      current_price?: number; unrealized_pnl?: number;
    }

    interface RealPositionsTableProps {
      positions: RealPosition[];
      onClosePosition: (position: RealPosition) => void;
      loading?: boolean;
    }

    export const RealPositionsTable: React.FC<RealPositionsTableProps> = (props) => {
      // Table voi columns: Ma CK, San, Gia vao, SL, Gia hien tai, P&L (unrealized), Ngay mo, Action
      // Action: button "Dong vi the" -> onClosePosition(position)
      // P&L hien mau xanh khi > 0, do khi < 0
      // Empty state: "Chua co vi the nao"
      // Tailwind: table voi hover row, responsive
    };
    ```
  </action>
  <verify>
    <automated>test -f ai-stoploss-engine-fe/components/portfolio/RealOrderForm.tsx && test -f ai-stoploss-engine-fe/components/portfolio/CashBalanceCard.tsx && test -f ai-stoploss-engine-fe/components/portfolio/RealPositionsTable.tsx && grep -q "realPortfolioApi" ai-stoploss-engine-fe/services/api.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File components/portfolio/RealOrderForm.tsx chua "RealOrderForm" component
    - RealOrderForm co 6 form fields: symbol, exchange, side, quantity, filled_price, filled_date
    - RealOrderForm goi "real-orders" endpoint
    - File components/portfolio/CashBalanceCard.tsx chua "CashBalanceCard" component
    - CashBalanceCard hien thi 4 metrics: totalBalance, availableCash, pendingSettlement, deployedCash
    - File components/portfolio/RealPositionsTable.tsx chua "RealPositionsTable" component
    - RealPositionsTable co button "Dong vi the"
    - api.ts chua realPortfolioApi voi 4 methods: createOrder, getTransactionHistory, getOpenPositions, closePosition
    - VERIFY: GET /api/portfolios/:id response chua available_cash va pending_settlement_cash (migration 007 da them fields nay). Neu Portfolio model chi SELECT specific columns, phai them available_cash va pending_settlement_cash vao SELECT list.
  </acceptance_criteria>
  <done>API service updated, RealOrderForm voi 6 fields + auto-fee, CashBalanceCard voi 4 metrics, RealPositionsTable voi close action, portfolio endpoint verified tra ve cash fields</done>
</task>

<task type="auto">
  <name>Task 2: ClosePositionModal, TransactionHistory, va Update PortfolioView voi Tab Real/Paper</name>
  <files>
    ai-stoploss-engine-fe/components/portfolio/ClosePositionModal.tsx,
    ai-stoploss-engine-fe/components/portfolio/TransactionHistory.tsx,
    ai-stoploss-engine-fe/components/PortfolioView.tsx
  </files>
  <read_first>
    - ai-stoploss-engine-fe/components/PortfolioView.tsx
    - ai-stoploss-engine-fe/components/portfolio/RealOrderForm.tsx
    - ai-stoploss-engine-fe/components/portfolio/CashBalanceCard.tsx
    - ai-stoploss-engine-fe/components/portfolio/RealPositionsTable.tsx
    - ai-stoploss-engine-fe/services/api.ts
  </read_first>
  <action>
    **1. ClosePositionModal.tsx** (`components/portfolio/ClosePositionModal.tsx`):
    Per D-10:
    ```typescript
    interface ClosePositionModalProps {
      position: { id: string; symbol: string; entry_price: number; quantity: number; } | null;
      portfolioId: string;
      isOpen: boolean;
      onClose: () => void;
      onSuccess: () => void;
    }

    export const ClosePositionModal: React.FC<ClosePositionModalProps> = (props) => {
      // Modal overlay voi form:
      //   Header: "Dong vi the {symbol}"
      //   Info: Gia vao {entry_price}, So luong {quantity}
      //   Input: Gia ban (number, required)
      //   Input: Ngay ban (date, default today)
      //   Auto-calculate: Gross P&L = (sellPrice - entryPrice) * quantity
      //   Auto-calculate: Phi ban = sellPrice * qty * 0.0015
      //   Auto-calculate: Thue ban = sellPrice * qty * 0.001
      //   Auto-calculate: Net P&L = Gross - Phi ban - Thue ban - (entryPrice * qty * 0.0015 as buy fee)
      //   Net P&L hien mau xanh/do
      //   Button: "Xac nhan dong" va "Huy"
      //   Submit: POST close endpoint -> onSuccess()
      // Tailwind: modal overlay fixed inset-0, card centered, z-50
    };
    ```

    **2. TransactionHistory.tsx** (`components/portfolio/TransactionHistory.tsx`):
    ```typescript
    interface TransactionHistoryProps {
      portfolioId: string;
    }

    export const TransactionHistory: React.FC<TransactionHistoryProps> = ({ portfolioId }) => {
      // State: orders, page, total, loading
      // useEffect: fetch GET /real-orders?page=1
      // Table: Ngay, Loai (MUA/BAN), Ma CK, San, SL, Gia, Tong GT, Phi, Trang thai
      // Loai MUA = badge xanh, BAN = badge do
      // Pagination: Previous/Next buttons
      // Empty state: "Chua co giao dich nao"
      // Format VND voi toLocaleString('vi-VN')
    };
    ```

    **3. Update PortfolioView.tsx** -- them tab system va integrate components:
    Doc ky PortfolioView.tsx hien tai. Them:

    a. Tab state:
    ```typescript
    const [activeTab, setActiveTab] = useState<'REAL' | 'PAPER'>('REAL');
    ```

    b. Tab UI (ngay duoi header cua portfolio view):
    ```tsx
    <div className="flex gap-2 mb-4">
      <button
        onClick={() => setActiveTab('REAL')}
        className={`px-4 py-2 rounded-lg font-medium ${
          activeTab === 'REAL'
            ? 'bg-blue-600 text-white'
            : 'bg-gray-700 text-gray-300 hover:bg-gray-600'
        }`}
      >
        Portfolio That
      </button>
      <button
        onClick={() => setActiveTab('PAPER')}
        className={`px-4 py-2 rounded-lg font-medium ${
          activeTab === 'PAPER'
            ? 'bg-purple-600 text-white'
            : 'bg-gray-700 text-gray-300 hover:bg-gray-600'
        }`}
      >
        Paper Trading
      </button>
    </div>
    ```

    c. Conditional render:
    ```tsx
    {activeTab === 'REAL' && (
      <div className="space-y-6">
        <CashBalanceCard totalBalance={...} availableCash={...} pendingSettlement={...} />
        <RealOrderForm portfolioId={portfolioId} availableCash={...} onSuccess={refetch} />
        <RealPositionsTable positions={realPositions} onClosePosition={setClosingPosition} />
        <TransactionHistory portfolioId={portfolioId} />
        <ClosePositionModal
          position={closingPosition}
          portfolioId={portfolioId}
          isOpen={!!closingPosition}
          onClose={() => setClosingPosition(null)}
          onSuccess={refetch}
        />
      </div>
    )}
    {activeTab === 'PAPER' && (
      // Render existing PortfolioView content (paper trading) o day
      // Di chuyen existing JSX vao day
    )}
    ```

    d. Fetch real data:
    ```typescript
    const [realPositions, setRealPositions] = useState([]);
    const [cashBalance, setCashBalance] = useState({ total_balance: 0, available_cash: 0, pending_settlement_cash: 0 });

    // Fetch khi activeTab === 'REAL' va co portfolioId
    useEffect(() => {
      if (activeTab === 'REAL' && portfolioId) {
        realPortfolioApi.getOpenPositions(portfolioId).then(res => setRealPositions(res.data.data));
        // Cash balance from portfolio data (already fetched)
      }
    }, [activeTab, portfolioId]);
    ```

    **NOTE:** Giu nguyen TOAN BO code hien tai cua PortfolioView. Chi WRAP no trong `{activeTab === 'PAPER' && ...}`. THEM code moi cho `{activeTab === 'REAL' && ...}`.
  </action>
  <verify>
    <automated>test -f ai-stoploss-engine-fe/components/portfolio/ClosePositionModal.tsx && test -f ai-stoploss-engine-fe/components/portfolio/TransactionHistory.tsx && grep -q "activeTab" ai-stoploss-engine-fe/components/PortfolioView.tsx && grep -q "REAL" ai-stoploss-engine-fe/components/PortfolioView.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File ClosePositionModal.tsx chua modal voi sell_price va sell_date inputs
    - ClosePositionModal tu dong tinh P&L (gross va net)
    - File TransactionHistory.tsx chua table voi pagination
    - PortfolioView.tsx chua "activeTab" state
    - PortfolioView.tsx chua tab buttons "Portfolio That" va "Paper Trading"
    - PortfolioView.tsx import va render RealOrderForm, CashBalanceCard, RealPositionsTable, ClosePositionModal, TransactionHistory
    - Paper trading content cua PortfolioView KHONG bi xoa (wrapped trong PAPER tab)
  </acceptance_criteria>
  <done>ClosePositionModal + TransactionHistory created, PortfolioView co tab Real/Paper, tat ca components integrated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Portfolio Management UI</name>
  <files>ai-stoploss-engine-fe/components/PortfolioView.tsx</files>
  <action>
    Day la checkpoint de user verify UI. Khong co code change -- chi verify ket qua cua Task 1 va Task 2.
    User se start backend + frontend va kiem tra toan bo flow:
    - Tab Real/Paper hien thi dung
    - Form nhap lenh hoat dong
    - Cash balance card hien so lieu
    - Danh sach vi the va dong vi the hoat dong
    - Paper trading tab van nhu cu
  </action>
  <verify>
    <automated>echo "CHECKPOINT: Human verification required"</automated>
  </verify>
  <acceptance_criteria>
    - User xac nhan tab Real/Paper hien thi
    - User xac nhan form nhap lenh hoat dong
    - User xac nhan paper trading khong bi break
  </acceptance_criteria>
  <done>User approved UI hoac cung cap feedback de fix</done>
</task>

</tasks>

<verification>
- Frontend components render khong loi
- API calls toi backend thanh cong
- Tab Real/Paper chuyen doi muot
- Paper trading UI khong bi break
- GET /api/portfolios/:id tra ve available_cash va pending_settlement_cash
</verification>

<success_criteria>
1. RealOrderForm co 6 fields + auto-fee + available cash display
2. CashBalanceCard hien 4 metrics (tong von, kha dung, cho TT, da deploy)
3. RealPositionsTable hien danh sach + nut dong vi the
4. ClosePositionModal tinh P&L voi phi + thue
5. TransactionHistory co pagination
6. PortfolioView co tab Real/Paper, paper trading khong bi anh huong
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-portfolio-management/01-05-SUMMARY.md`
</output>
