---
phase: 03-ai-enhancement
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - ai-stoploss-engine-be/services/ai/dynamicStopLoss.js
  - ai-stoploss-engine-be/workers/stopLossMonitor.js
  - ai-stoploss-engine-be/services/aiService.js
  - ai-stoploss-engine-be/tests/services/dynamicStopLoss.test.js
autonomous: true
requirements: [AISL-01, AISL-03, AISL-05, AISL-06]

must_haves:
  truths:
    - "SL tu dong dieu chinh theo ATR hien tai (khong chi ATR luc dat lenh)"
    - "Trailing stop mo rong khi volatility cao, thu hep khi thap"
    - "Worker recalculate SL moi 5 phut trong gio giao dich"
    - "AI narrative giai thich TAI SAO SL thay doi (Gemini + fallback rule-based)"
    - "SL chi duoc move len (LONG) hoac xuong (SHORT) -- KHONG BAO GIO ha SL"
  artifacts:
    - path: "ai-stoploss-engine-be/services/ai/dynamicStopLoss.js"
      provides: "Dynamic SL calculation + narrative generation"
      exports: ["calculateDynamicSL", "generateSLNarrative", "generateFallbackNarrative"]
    - path: "ai-stoploss-engine-be/workers/stopLossMonitor.js"
      provides: "Upgraded worker with 5-min dynamic SL cron"
    - path: "ai-stoploss-engine-be/tests/services/dynamicStopLoss.test.js"
      provides: "Tests for dynamic SL logic"
  key_links:
    - from: "ai-stoploss-engine-be/services/ai/dynamicStopLoss.js"
      to: "ai-stoploss-engine-be/services/ai/indicatorCache.js"
      via: "import getOrCreateIndicators, feedCandle"
      pattern: "import.*indicatorCache"
    - from: "ai-stoploss-engine-be/services/ai/dynamicStopLoss.js"
      to: "ai-stoploss-engine-be/services/ai/regimeDetector.js"
      via: "import detectRegime, REGIME_MULTIPLIERS"
      pattern: "import.*regimeDetector"
    - from: "ai-stoploss-engine-be/workers/stopLossMonitor.js"
      to: "ai-stoploss-engine-be/services/ai/dynamicStopLoss.js"
      via: "import calculateDynamicSL"
      pattern: "import.*dynamicStopLoss"
---

<objective>
Tao Dynamic Stop Loss service va upgrade stopLossMonitor worker de recalculate SL moi 5 phut dua tren ATR streaming + regime-adaptive multiplier.

Purpose: SL phan ung voi thay doi volatility real-time thay vi tinh 1 lan luc dat lenh. Trailing stop thong minh mo rong/thu hep theo regime.
Output: dynamicStopLoss.js service + upgraded stopLossMonitor.js + tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ai-enhancement/03-CONTEXT.md
@.planning/phases/03-ai-enhancement/03-RESEARCH.md
@.planning/phases/03-ai-enhancement/03-01-SUMMARY.md

@ai-stoploss-engine-be/workers/stopLossMonitor.js
@ai-stoploss-engine-be/services/aiService.js
@ai-stoploss-engine-be/services/ai/indicatorCache.js
@ai-stoploss-engine-be/services/ai/regimeDetector.js
@ai-stoploss-engine-be/services/shared/priceBandValidator.js
@ai-stoploss-engine-be/models/Position.js

<interfaces>
<!-- From Plan 01 outputs -->
From services/ai/indicatorCache.js:
- getOrCreateIndicators(symbol, period=14) -> { atr, bb, sma50, sma200, lastUpdate, regime, regimeTimestamp }
- feedCandle(symbol, candle) -> void (candle = { high, low, close })

From services/ai/regimeDetector.js:
- detectRegime(indicators) -> 'VOLATILE' | 'BULLISH' | 'BEARISH' | 'SIDEWAYS'
- REGIME_MULTIPLIERS = { VOLATILE: 2.5, BULLISH: 1.5, BEARISH: 1.5, SIDEWAYS: 1.0 }

From services/shared/priceBandValidator.js:
- clampToBand(price, refPrice, exchange) -> number (clamped + snapped to tick)
- getFloorPrice(refPrice, exchange) -> number
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create dynamicStopLoss.js service + tests</name>
  <files>
    ai-stoploss-engine-be/services/ai/dynamicStopLoss.js,
    ai-stoploss-engine-be/tests/services/dynamicStopLoss.test.js
  </files>
  <read_first>
    ai-stoploss-engine-be/services/ai/indicatorCache.js,
    ai-stoploss-engine-be/services/ai/regimeDetector.js,
    ai-stoploss-engine-be/services/shared/priceBandValidator.js,
    ai-stoploss-engine-be/services/aiService.js,
    ai-stoploss-engine-be/services/tickSizeEngine.js,
    ai-stoploss-engine-be/tests/helpers/indicatorMocks.js
  </read_first>
  <behavior>
    - calculateDynamicSL(position, candle, indicators): tra ve { newSL, regime, atrValue, multiplier, changed, reason }
    - LONG position: newSL = close - ATR * multiplier. newSL = max(newSL, currentSL) -- trailing chi tang
    - SHORT position: newSL = close + ATR * multiplier. newSL = min(newSL, currentSL) -- trailing chi giam
    - SL clamp trong price band (HOSE 7%, HNX 10%) via clampToBand
    - changed = false khi newSL === currentSL (trailing khong move)
    - generateSLNarrative(data) goi Gemini voi timeout 5s, fallback rule-based khi timeout
    - generateFallbackNarrative(data) tao text tu template: "SL dieu chinh tu X -> Y do ATR={atr}, regime={regime}..."
  </behavior>
  <action>
    1. TAO TEST TRUOC (TDD RED): `tests/services/dynamicStopLoss.test.js`
       - Mock indicatorCache, regimeDetector, priceBandValidator
       - Test calculateDynamicSL LONG: newSL = close - ATR * multiplier, trailing chi tang
       - Test calculateDynamicSL SHORT: newSL = close + ATR * multiplier, trailing chi giam
       - Test SL clamp: khi SL vuot price band, duoc clamp lai
       - Test changed=false khi trailing khong move (newSL < currentSL cho LONG)
       - Test generateFallbackNarrative tra ve string co chua regime va ATR info
       - Run tests -- MUST fail (RED)

    2. TAO SERVICE (TDD GREEN): `services/ai/dynamicStopLoss.js`
       - Import getOrCreateIndicators, feedCandle from indicatorCache
       - Import detectRegime, REGIME_MULTIPLIERS from regimeDetector
       - Import clampToBand from priceBandValidator
       - Import snapToTickSize from tickSizeEngine
       - Export calculateDynamicSL(position, candle, indicators):
         * isLong = (pos.side || 'LONG') === 'LONG'
         * regime = detectRegime(indicators)
         * multiplier = REGIME_MULTIPLIERS[regime]
         * atrValue = Number(indicators.atr.getResult())
         * Tinh newSL, apply trailing logic (chi move LONG len, SHORT xuong)
         * Clamp to price band via clampToBand(newSL, candle.close, pos.exchange || 'HOSE')
         * Snap to tick size
         * Return { newSL, oldSL: currentSL, regime, atrValue, multiplier, changed: newSL !== currentSL, reason }
       - Export generateSLNarrative(data): goi callGeminiJSON voi timeout 5s (per D-07). Prompt yeu cau Gemini giai thich TAI SAO SL thay doi bang tieng Viet. Wrap trong Promise.race voi 5s timeout. Catch -> generateFallbackNarrative(data)
       - Export generateFallbackNarrative(data): template string: "Stop Loss dieu chinh {oldSL} -> {newSL}. Ly do: ATR hien tai {atrValue} VND, regime {regime} (multiplier {multiplier}x). {extra}" voi extra tuy regime

    3. Run tests -- MUST pass (GREEN)
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-be && npx vitest run tests/services/dynamicStopLoss.test.js --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function calculateDynamicSL" ai-stoploss-engine-be/services/ai/dynamicStopLoss.js
    - grep -q "export function generateSLNarrative" ai-stoploss-engine-be/services/ai/dynamicStopLoss.js
    - grep -q "export function generateFallbackNarrative" ai-stoploss-engine-be/services/ai/dynamicStopLoss.js
    - grep -q "REGIME_MULTIPLIERS" ai-stoploss-engine-be/services/ai/dynamicStopLoss.js
    - grep -q "clampToBand" ai-stoploss-engine-be/services/ai/dynamicStopLoss.js
    - grep -q "trailing" ai-stoploss-engine-be/tests/services/dynamicStopLoss.test.js
    - grep -q "clamp" ai-stoploss-engine-be/tests/services/dynamicStopLoss.test.js
    - grep -q "narrative" ai-stoploss-engine-be/tests/services/dynamicStopLoss.test.js
  </acceptance_criteria>
  <done>
    dynamicStopLoss.js tra ve SL moi dua tren ATR * regime_multiplier. Trailing logic chi cho phep move co loi cho user. SL clamp trong price band. Narrative co Gemini + fallback. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade stopLossMonitor worker voi dynamic SL cron 5 phut</name>
  <files>
    ai-stoploss-engine-be/workers/stopLossMonitor.js,
    ai-stoploss-engine-be/services/aiService.js
  </files>
  <read_first>
    ai-stoploss-engine-be/workers/stopLossMonitor.js,
    ai-stoploss-engine-be/services/ai/dynamicStopLoss.js,
    ai-stoploss-engine-be/services/ai/indicatorCache.js,
    ai-stoploss-engine-be/models/Position.js,
    ai-stoploss-engine-be/models/ExecutionLog.js
  </read_first>
  <action>
    1. Mo rong stopLossMonitor.js (KHONG rewrite -- them vao existing code):
       - Import calculateDynamicSL, generateSLNarrative from '../services/ai/dynamicStopLoss.js'
       - Import getOrCreateIndicators, feedCandle from '../services/ai/indicatorCache.js'

    2. Them cron job moi (per D-01): `cron.schedule('*/5 9-15 * * 1-5', recalculateDynamicSLJob)`
       - Chay moi 5 phut, 9h-15h, Mon-Fri (gio giao dich VN)
       - Function recalculateDynamicSLJob():
         a. Query positions WHERE status='OPEN' AND stop_loss IS NOT NULL -- ca REAL va PAPER
         b. Group by symbol (giong pattern hien tai trong stopLossMonitor)
         c. Voi moi symbol: fetch candle moi nhat, feedCandle() de update indicators
         d. Voi moi position: goi calculateDynamicSL(position, candle, indicators)
         e. Neu changed=true: update Position.update(pos.id, { stopLoss: newSL })
         f. Log vao ExecutionLog voi type='DYNAMIC_SL_UPDATE'
         g. Goi generateSLNarrative(data) va broadcast via WebSocket
         h. Gioi han concurrent API calls (max 5 symbols dong thoi) de tranh VPBS rate limit

    3. Khong thay doi existing cron jobs (CHECK_CRON, ALERT_CRON) -- chi THEM cron moi

    4. Trong aiService.js: export callGeminiJSON de dynamicStopLoss.js co the import.
       Tim function callGeminiJSON va them `export` keyword phia truoc neu chua co.
       KHONG thay doi logic ben trong callGeminiJSON.
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-be && node -e "import('./workers/stopLossMonitor.js').catch(e => { if(e.message.includes('database') || e.message.includes('ECONNREFUSED') || e.message.includes('dotenv')) { console.log('Worker imports OK (DB connection expected to fail in test)'); process.exit(0); } else { console.error(e); process.exit(1); } })"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "recalculateDynamicSL" ai-stoploss-engine-be/workers/stopLossMonitor.js
    - grep -q "dynamicStopLoss" ai-stoploss-engine-be/workers/stopLossMonitor.js
    - grep -q "indicatorCache" ai-stoploss-engine-be/workers/stopLossMonitor.js
    - grep -q "*/5 9-15" ai-stoploss-engine-be/workers/stopLossMonitor.js
    - grep -q "DYNAMIC_SL_UPDATE" ai-stoploss-engine-be/workers/stopLossMonitor.js
    - grep -q "export.*callGeminiJSON\|export async function callGeminiJSON\|export function callGeminiJSON" ai-stoploss-engine-be/services/aiService.js
  </acceptance_criteria>
  <done>
    stopLossMonitor.js co them cron job 5-min cho dynamic SL recalculation. Worker import dynamicStopLoss + indicatorCache. callGeminiJSON exported tu aiService.js. Existing functionality khong bi anh huong.
  </done>
</task>

</tasks>

<verification>
- `cd ai-stoploss-engine-be && npx vitest run tests/services/dynamicStopLoss.test.js --reporter=verbose` — all tests pass
- `grep -c "cron.schedule" ai-stoploss-engine-be/workers/stopLossMonitor.js` — should be 3+ (existing + new dynamic SL cron)
- Existing tests still pass: `cd ai-stoploss-engine-be && npx vitest run --reporter=verbose`
</verification>

<success_criteria>
- Dynamic SL service tinh dung newSL = close - ATR * regime_multiplier
- Trailing logic: SL chi move co loi (LONG tang, SHORT giam)
- SL clamp trong price band
- Worker co cron 5-min moi, khong break existing crons
- Narrative co Gemini (5s timeout) + fallback rule-based
- callGeminiJSON exported tu aiService.js
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-enhancement/03-02-SUMMARY.md`
</output>
