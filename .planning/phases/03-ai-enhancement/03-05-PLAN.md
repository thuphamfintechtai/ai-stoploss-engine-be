---
phase: 03-ai-enhancement
plan: 05
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - ai-stoploss-engine-be/controllers/ai.controller.js
  - ai-stoploss-engine-be/services/ai/rebalancingSuggestion.js
  - ai-stoploss-engine-be/routes/ai.routes.js
  - ai-stoploss-engine-be/tests/services/rebalancing.test.js
autonomous: true
requirements: [AICAP-03]

must_haves:
  truths:
    - "Rebalancing suggestions canh bao khi 1 sector > 30% portfolio"
    - "Gemini narrative cho rebalancing voi fallback rule-based"
    - "API endpoint tra ve sector concentration + suggestions"
  artifacts:
    - path: "ai-stoploss-engine-be/services/ai/rebalancingSuggestion.js"
      provides: "Sector concentration analysis + rebalancing suggestions"
      exports: ["generateRebalancingSuggestions"]
    - path: "ai-stoploss-engine-be/tests/services/rebalancing.test.js"
      provides: "Tests for rebalancing logic"
  key_links:
    - from: "ai-stoploss-engine-be/services/ai/rebalancingSuggestion.js"
      to: "ai-stoploss-engine-be/services/ai/sectorClassification.js"
      via: "import getSector, SECTOR_LABELS"
      pattern: "import.*sectorClassification"
    - from: "ai-stoploss-engine-be/services/ai/rebalancingSuggestion.js"
      to: "ai-stoploss-engine-be/services/aiService.js"
      via: "import callGeminiJSON for narrative"
      pattern: "callGeminiJSON"
---

<objective>
Tao rebalancing suggestion service voi sector concentration warning va Gemini narrative. Them API endpoint.

Purpose: User biet khi nao portfolio bi tap trung qua nhieu vao 1 nganh (vi du banking > 30%) va nhan goi y dieu chinh.
Output: rebalancingSuggestion.js service + API endpoint + tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ai-enhancement/03-CONTEXT.md
@.planning/phases/03-ai-enhancement/03-RESEARCH.md
@.planning/phases/03-ai-enhancement/03-01-SUMMARY.md

@ai-stoploss-engine-be/services/ai/sectorClassification.js
@ai-stoploss-engine-be/services/aiService.js
@ai-stoploss-engine-be/controllers/ai.controller.js
@ai-stoploss-engine-be/routes/ai.routes.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create rebalancingSuggestion.js service + tests</name>
  <files>
    ai-stoploss-engine-be/services/ai/rebalancingSuggestion.js,
    ai-stoploss-engine-be/tests/services/rebalancing.test.js
  </files>
  <read_first>
    ai-stoploss-engine-be/services/ai/sectorClassification.js,
    ai-stoploss-engine-be/services/ai/capitalAllocation.js,
    ai-stoploss-engine-be/services/aiService.js,
    ai-stoploss-engine-be/config/database.js
  </read_first>
  <behavior>
    - generateRebalancingSuggestions(positions, totalPortfolioValue): return { sectorBreakdown, warnings, suggestions, narrative }
    - sectorBreakdown: array of { sector, sectorLabel, totalValue, percent, symbols[] }
    - warnings: sectors voi percent > 30 (per D-18 fallback threshold)
    - suggestions: rule-based goi y "HPG chiem 40% portfolio -- xem xet giam"
    - narrative: Gemini text hoac fallback rule-based khi timeout 5s (per D-07, D-18)
    - Empty positions -> return empty breakdown, no warnings
    - Positions khong co market_value -> skip
  </behavior>
  <action>
    1. TAO TEST TRUOC (TDD RED): `tests/services/rebalancing.test.js`
       - Mock positions: 3 banking stocks (60% portfolio), 2 tech stocks (40%)
       - Test: sectorBreakdown co 2 entries (BANKING, TECHNOLOGY)
       - Test: warnings co BANKING (> 30%)
       - Test: suggestions co text canh bao banking
       - Test: empty positions -> empty breakdown, no warnings
       - Test fallback narrative (mock Gemini timeout)
       - Run -- MUST fail (RED)

    2. TAO SERVICE (TDD GREEN): `services/ai/rebalancingSuggestion.js`
       - Import { getSector, SECTOR_LABELS } from './sectorClassification.js'
       - Import callGeminiJSON from '../aiService.js' (da export tu Plan 02)

       - Export async generateRebalancingSuggestions(positions, totalPortfolioValue):
         * Group positions by sector via getSector(symbol)
         * Tinh totalValue per sector = sum(market_value hoac entry_price * quantity)
         * sectorBreakdown = sorted by percent DESC
         * warnings = sectors where percent > 30 (per D-18)
         * suggestions = rule-based: cho moi warning, generate "{symbol} chiem {x}% portfolio - xem xet giam xuong duoi 30%"
         * narrative: goi Gemini voi sector data, timeout 5s via Promise.race
         * Fallback narrative: list warnings bang tieng Viet
         * Return { sectorBreakdown, warnings, suggestions, narrative }

    3. Run tests -- MUST pass (GREEN)
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-be && npx vitest run tests/services/rebalancing.test.js --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export async function generateRebalancingSuggestions" ai-stoploss-engine-be/services/ai/rebalancingSuggestion.js
    - grep -q "getSector" ai-stoploss-engine-be/services/ai/rebalancingSuggestion.js
    - grep -q "SECTOR_LABELS" ai-stoploss-engine-be/services/ai/rebalancingSuggestion.js
    - grep -q "30" ai-stoploss-engine-be/services/ai/rebalancingSuggestion.js
    - grep -q "sectorBreakdown" ai-stoploss-engine-be/tests/services/rebalancing.test.js
    - grep -q "warnings" ai-stoploss-engine-be/tests/services/rebalancing.test.js
  </acceptance_criteria>
  <done>
    rebalancingSuggestion.js group positions by sector, canh bao > 30%, generate suggestions + Gemini narrative voi fallback. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add rebalancing API endpoint + wire to routes</name>
  <files>
    ai-stoploss-engine-be/controllers/ai.controller.js,
    ai-stoploss-engine-be/routes/ai.routes.js
  </files>
  <read_first>
    ai-stoploss-engine-be/controllers/ai.controller.js,
    ai-stoploss-engine-be/routes/ai.routes.js,
    ai-stoploss-engine-be/services/ai/rebalancingSuggestion.js,
    ai-stoploss-engine-be/models/Position.js
  </read_first>
  <action>
    1. Trong ai.controller.js, them handler:

       getRebalancingSuggestions(req, res, next):
       - Query: { portfolio_id } (required)
       - Fetch open positions cho portfolio_id: Position.findAll({ portfolio_id, status: 'OPEN' }) hoac query truc tiep
       - Fetch portfolio totalBalance
       - Tinh totalPortfolioValue = sum of (entry_price * quantity) cho tat ca open positions
       - Goi generateRebalancingSuggestions(positions, totalPortfolioValue)
       - Return { success: true, data: result }

    2. Export getRebalancingSuggestions tu controller

    3. Trong ai.routes.js, them route:
       - GET /api/ai/rebalancing?portfolio_id=xxx -> getRebalancingSuggestions

    4. KHONG thay doi existing routes/handlers
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-be && grep -n "rebalancing" routes/ai.routes.js controllers/ai.controller.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "rebalancing" ai-stoploss-engine-be/routes/ai.routes.js
    - grep -q "getRebalancingSuggestions" ai-stoploss-engine-be/controllers/ai.controller.js
    - grep -q "generateRebalancingSuggestions" ai-stoploss-engine-be/controllers/ai.controller.js
  </acceptance_criteria>
  <done>
    GET /api/ai/rebalancing endpoint tra ve sector breakdown + warnings + suggestions + narrative. Authentication required.
  </done>
</task>

</tasks>

<verification>
- `cd ai-stoploss-engine-be && npx vitest run tests/services/rebalancing.test.js --reporter=verbose` — all tests pass
- `grep "rebalancing" ai-stoploss-engine-be/routes/ai.routes.js` — route exists
- Existing tests still pass: `cd ai-stoploss-engine-be && npx vitest run --reporter=verbose`
</verification>

<success_criteria>
- Rebalancing service group positions by sector dung
- Warning khi sector > 30% portfolio
- Gemini narrative voi 5s timeout + fallback rule-based
- API endpoint hoat dong, authentication required
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-enhancement/03-05-SUMMARY.md`
</output>
