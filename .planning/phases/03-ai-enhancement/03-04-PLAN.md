---
phase: 03-ai-enhancement
plan: 04
type: execute
wave: 3
depends_on: [03-01]
files_modified:
  - ai-stoploss-engine-be/services/ai/capitalAllocation.js
  - ai-stoploss-engine-be/controllers/ai.controller.js
  - ai-stoploss-engine-be/routes/ai.routes.js
  - ai-stoploss-engine-be/tests/services/capitalAllocation.test.js
autonomous: true
requirements: [AICAP-01, AICAP-02]

must_haves:
  truths:
    - "AI suggest position sizing tu half-Kelly dua tren win rate + avg R:R"
    - "Risk budget visualization data: phan tram ngan sach rui ro da dung"
    - "Win rate tinh tu closed positions ca REAL va PAPER"
    - "Minimum 10 closed positions de tinh Kelly -- warning khi < 10"
  artifacts:
    - path: "ai-stoploss-engine-be/services/ai/capitalAllocation.js"
      provides: "Half-Kelly position sizing + risk budget calculation"
      exports: ["calculateHalfKelly", "calculateRiskBudget", "getTradeStats"]
    - path: "ai-stoploss-engine-be/tests/services/capitalAllocation.test.js"
      provides: "Tests for Kelly criterion and risk budget"
  key_links:
    - from: "ai-stoploss-engine-be/services/ai/capitalAllocation.js"
      to: "ai-stoploss-engine-be/config/database.js"
      via: "query for closed positions stats"
      pattern: "query.*positions.*CLOSED"
    - from: "ai-stoploss-engine-be/routes/ai.routes.js"
      to: "ai-stoploss-engine-be/controllers/ai.controller.js"
      via: "new routes for position-sizing and risk-budget"
      pattern: "position-sizing|risk-budget"
---

<objective>
Tao Capital Allocation service voi half-Kelly position sizing va risk budget calculation. Them API endpoints cho frontend.

Purpose: User co du lieu thong ke de quyet dinh position size hop ly (khong qua lon, khong qua nho). Risk budget cho thay toan canh rui ro portfolio.
Output: capitalAllocation.js service + 2 API endpoints + tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ai-enhancement/03-CONTEXT.md
@.planning/phases/03-ai-enhancement/03-RESEARCH.md
@.planning/phases/03-ai-enhancement/03-01-SUMMARY.md

@ai-stoploss-engine-be/controllers/ai.controller.js
@ai-stoploss-engine-be/routes/ai.routes.js
@ai-stoploss-engine-be/services/riskCalculator.js
@ai-stoploss-engine-be/config/database.js
@ai-stoploss-engine-be/models/Position.js

<interfaces>
From services/ai/sectorClassification.js (Plan 01):
- getSector(symbol) -> string ('BANKING' | 'REAL_ESTATE' | ... | 'OTHER')
- SECTOR_LABELS -> { BANKING: 'Ngan hang', ... }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create capitalAllocation.js service + tests</name>
  <files>
    ai-stoploss-engine-be/services/ai/capitalAllocation.js,
    ai-stoploss-engine-be/tests/services/capitalAllocation.test.js
  </files>
  <read_first>
    ai-stoploss-engine-be/services/riskCalculator.js,
    ai-stoploss-engine-be/config/database.js,
    ai-stoploss-engine-be/models/Position.js,
    ai-stoploss-engine-be/services/ai/sectorClassification.js,
    ai-stoploss-engine-be/tests/services/regimeDetector.test.js
  </read_first>
  <behavior>
    - calculateHalfKelly(winRate, avgWinLoss): f* = (p*b-q)/b * 0.5, cap at 25%, floor at 0
    - kelly <= 0 -> interpretation "Negative expectancy -- khong nen tang vi the"
    - kelly > 0 -> interpretation "Nen dau tu X% von vao vi the nay"
    - getTradeStats(portfolioId): query closed positions, return { totalTrades, wins, losses, winRate, avgWin, avgLoss, avgWinLoss } per context (REAL/PAPER)
    - getTradeStats voi < 10 trades -> warning "Chua du du lieu (can >= 10 giao dich dong)"
    - calculateRiskBudget(portfolioId, totalBalance, maxRiskPercent): return { usedRiskVnd, usedRiskPercent, remainingBudget, positions[] voi risk per position }
  </behavior>
  <action>
    1. TAO TEST TRUOC (TDD RED): `tests/services/capitalAllocation.test.js`
       - Test calculateHalfKelly(0.6, 2.0) -> halfKelly > 0, recommended_percent > 0, capped at 25
       - Test calculateHalfKelly(0.3, 0.5) -> kelly <= 0, halfKelly = 0, interpretation contains "Negative"
       - Test calculateHalfKelly(0.5, 1.0) -> kelly = 0, halfKelly = 0
       - Test calculateHalfKelly edge: winRate = 0 -> halfKelly = 0
       - Test calculateHalfKelly edge: winRate = 1 -> capped at 25%
       - Mock database query cho getTradeStats tests:
         * Mock voi 20 trades -> return valid stats
         * Mock voi 5 trades -> return stats voi warning
       - Test calculateRiskBudget voi mock positions
       - Run -- MUST fail (RED)

    2. TAO SERVICE (TDD GREEN): `services/ai/capitalAllocation.js`
       - Import { query } from '../../config/database.js'
       - Import { getSector } from './sectorClassification.js'

       - Export calculateHalfKelly(winRate, avgWinLoss):
         * p = clamp(winRate, 0, 1), q = 1 - p, b = max(0.01, avgWinLoss)
         * kelly = (p * b - q) / b
         * halfKelly = max(0, kelly * 0.5) (per D-15)
         * recommended_percent = min(halfKelly * 100, 25) -- cap 25%
         * interpretation based on kelly (tieng Viet)
         * Return { kelly_fraction, half_kelly, recommended_percent, interpretation }

       - Export async getTradeStats(portfolioId):
         * SQL query (per D-16): SELECT context, COUNT(*), wins, losses, avg_win, avg_loss FROM positions WHERE portfolio_id=$1 AND status IN ('CLOSED_SL','CLOSED_TP','CLOSED_MANUAL') GROUP BY context
         * Parse results per context (REAL, PAPER)
         * Calculate winRate = wins/total, avgWinLoss = avg_win/avg_loss
         * If total < 10: add warning "Chua du du lieu (can >= 10 giao dich dong de tinh chinh xac)"
         * Return { byContext: { REAL: {...}, PAPER: {...} }, combined: {...}, warning? }

       - Export async calculateRiskBudget(portfolioId, totalBalance, maxRiskPercent=5):
         * Query open positions voi stop_loss IS NOT NULL
         * Tinh risk per position: (entry_price - stop_loss) * quantity (LONG)
         * usedRiskVnd = sum of all position risks
         * maxRiskVnd = totalBalance * maxRiskPercent / 100
         * usedRiskPercent = (usedRiskVnd / maxRiskVnd) * 100
         * Group positions by sector via getSector
         * Return { usedRiskVnd, usedRiskPercent, maxRiskVnd, remainingBudget: maxRiskVnd - usedRiskVnd, positions: [{symbol, sector, riskVnd, riskPercent}], sectorConcentration: [{sector, totalRiskVnd, percent}] }

    3. Run tests -- MUST pass (GREEN)
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-be && npx vitest run tests/services/capitalAllocation.test.js --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function calculateHalfKelly" ai-stoploss-engine-be/services/ai/capitalAllocation.js
    - grep -q "export async function getTradeStats" ai-stoploss-engine-be/services/ai/capitalAllocation.js
    - grep -q "export async function calculateRiskBudget" ai-stoploss-engine-be/services/ai/capitalAllocation.js
    - grep -q "half_kelly" ai-stoploss-engine-be/services/ai/capitalAllocation.js
    - grep -q "kelly" ai-stoploss-engine-be/tests/services/capitalAllocation.test.js
    - grep -q "budget" ai-stoploss-engine-be/tests/services/capitalAllocation.test.js
    - grep -q "warning" ai-stoploss-engine-be/tests/services/capitalAllocation.test.js
  </acceptance_criteria>
  <done>
    capitalAllocation.js tinh half-Kelly dung, cap 25%, floor 0. getTradeStats query closed positions per context. calculateRiskBudget tinh risk per position + sector concentration. Warning khi < 10 trades. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add API endpoints cho position-sizing va risk-budget</name>
  <files>
    ai-stoploss-engine-be/controllers/ai.controller.js,
    ai-stoploss-engine-be/routes/ai.routes.js
  </files>
  <read_first>
    ai-stoploss-engine-be/controllers/ai.controller.js,
    ai-stoploss-engine-be/routes/ai.routes.js,
    ai-stoploss-engine-be/services/ai/capitalAllocation.js,
    ai-stoploss-engine-be/middleware/validation.js
  </read_first>
  <action>
    1. Trong ai.controller.js, them 2 handlers:

       a. getPositionSizing(req, res, next):
          - Body: { portfolio_id, symbol? (optional, cho specific stock analysis) }
          - Validate: portfolio_id required (Joi)
          - Goi getTradeStats(portfolio_id) de lay win rate + avg R:R
          - Goi calculateHalfKelly(winRate, avgWinLoss) voi combined stats
          - Neu co symbol: them context "Voi ma {symbol}, nen dat {recommended_percent}% von"
          - Return { success: true, data: { kelly: kellyResult, stats: tradeStats } }

       b. getRiskBudget(req, res, next):
          - Query: { portfolio_id } (required)
          - Fetch portfolio totalBalance tu database (giong pattern hien co trong controller)
          - Goi calculateRiskBudget(portfolio_id, totalBalance, maxRiskPercent)
          - maxRiskPercent lay tu portfolio settings hoac default 5
          - Return { success: true, data: riskBudgetResult }

       c. Export 2 handlers: getPositionSizing, getRiskBudget

    2. Trong ai.routes.js, them 2 routes:
       - POST /api/ai/position-sizing -> getPositionSizing (co Joi validation)
       - GET /api/ai/risk-budget?portfolio_id=xxx -> getRiskBudget

    3. KHONG thay doi existing routes/handlers
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-be && grep -n "position-sizing\|risk-budget" routes/ai.routes.js controllers/ai.controller.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "position-sizing" ai-stoploss-engine-be/routes/ai.routes.js
    - grep -q "risk-budget" ai-stoploss-engine-be/routes/ai.routes.js
    - grep -q "getPositionSizing" ai-stoploss-engine-be/controllers/ai.controller.js
    - grep -q "getRiskBudget" ai-stoploss-engine-be/controllers/ai.controller.js
    - grep -q "calculateHalfKelly" ai-stoploss-engine-be/controllers/ai.controller.js
    - grep -q "calculateRiskBudget" ai-stoploss-engine-be/controllers/ai.controller.js
  </acceptance_criteria>
  <done>
    2 API endpoints moi cho position sizing va risk budget. POST /api/ai/position-sizing tra ve Kelly result + trade stats. GET /api/ai/risk-budget tra ve risk budget + sector concentration. Authentication required (inherits from router.use).
  </done>
</task>

</tasks>

<verification>
- `cd ai-stoploss-engine-be && npx vitest run tests/services/capitalAllocation.test.js --reporter=verbose` — all tests pass
- `grep "position-sizing" ai-stoploss-engine-be/routes/ai.routes.js` — route exists
- `grep "risk-budget" ai-stoploss-engine-be/routes/ai.routes.js` — route exists
- Existing tests still pass: `cd ai-stoploss-engine-be && npx vitest run --reporter=verbose`
</verification>

<success_criteria>
- Half-Kelly tinh dung, cap 25%, floor 0
- Trade stats query ca REAL va PAPER closed positions
- Warning khi < 10 closed trades
- Risk budget tinh risk per position + sector concentration
- 2 API endpoints hoat dong, authentication required
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-enhancement/03-04-SUMMARY.md`
</output>
