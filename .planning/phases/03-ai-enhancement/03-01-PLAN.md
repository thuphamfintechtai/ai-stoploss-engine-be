---
phase: 03-ai-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-stoploss-engine-be/package.json
  - ai-stoploss-engine-be/package-lock.json
  - ai-stoploss-engine-be/services/ai/indicatorCache.js
  - ai-stoploss-engine-be/services/ai/regimeDetector.js
  - ai-stoploss-engine-be/services/ai/sectorClassification.js
  - ai-stoploss-engine-be/services/shared/priceBandValidator.js
  - ai-stoploss-engine-be/tests/services/regimeDetector.test.js
  - ai-stoploss-engine-be/tests/helpers/indicatorMocks.js
autonomous: true
requirements: [AISL-02, AISL-04, AICAP-03]

must_haves:
  truths:
    - "Regime detection tra ve VOLATILE/BULLISH/BEARISH/SIDEWAYS tu BB percentile + SMA crossover"
    - "Indicator cache luu tru va stream data cho ATR/BB/SMA per symbol"
    - "Price band validator clamp gia trong bien do HOSE 7%, HNX 10%"
    - "Sector classification map VN stocks pho bien sang sector"
  artifacts:
    - path: "ai-stoploss-engine-be/services/ai/regimeDetector.js"
      provides: "Rule-based regime detection + multiplier table"
      exports: ["detectRegime", "REGIME_MULTIPLIERS"]
    - path: "ai-stoploss-engine-be/services/ai/indicatorCache.js"
      provides: "Streaming indicator cache per symbol"
      exports: ["getOrCreateIndicators", "feedCandle", "clearCache"]
    - path: "ai-stoploss-engine-be/services/ai/sectorClassification.js"
      provides: "VN stock sector mapping"
      exports: ["getSector", "VN_SECTOR_MAP", "SECTOR_LABELS"]
    - path: "ai-stoploss-engine-be/services/shared/priceBandValidator.js"
      provides: "Price band limits for HOSE/HNX"
      exports: ["getFloorPrice", "getCeilingPrice", "clampToband"]
    - path: "ai-stoploss-engine-be/tests/services/regimeDetector.test.js"
      provides: "Tests for regime detection logic"
  key_links:
    - from: "ai-stoploss-engine-be/services/ai/regimeDetector.js"
      to: "trading-signals BollingerBands + SMA"
      via: "indicator instances from indicatorCache"
      pattern: "bb\\.getResult|sma.*\\.getResult"
    - from: "ai-stoploss-engine-be/services/shared/priceBandValidator.js"
      to: "ai-stoploss-engine-be/services/tickSizeEngine.js"
      via: "snapToTickSize for rounding"
      pattern: "snapToTickSize"
---

<objective>
Install trading-signals + simple-statistics, tao shared foundation cho 3 AI modules: indicator streaming cache, rule-based regime detector, sector classification, price band validator.

Purpose: Tat ca 3 AI modules (Dynamic SL, Probability TP, Capital Allocation) can shared infrastructure nay. Tach ra Wave 1 de Plans 02-04 co the dung ngay.
Output: 6 service files + 2 test files + npm packages installed
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-enhancement/03-CONTEXT.md
@.planning/phases/03-ai-enhancement/03-RESEARCH.md

@ai-stoploss-engine-be/services/aiService.js
@ai-stoploss-engine-be/services/tickSizeEngine.js
@ai-stoploss-engine-be/services/riskCalculator.js
@ai-stoploss-engine-fe/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install npm packages + Create shared AI service modules</name>
  <files>
    ai-stoploss-engine-be/package.json,
    ai-stoploss-engine-be/services/ai/indicatorCache.js,
    ai-stoploss-engine-be/services/ai/regimeDetector.js,
    ai-stoploss-engine-be/services/ai/sectorClassification.js,
    ai-stoploss-engine-be/services/shared/priceBandValidator.js
  </files>
  <read_first>
    ai-stoploss-engine-be/package.json,
    ai-stoploss-engine-be/services/tickSizeEngine.js,
    ai-stoploss-engine-be/services/riskCalculator.js
  </read_first>
  <action>
    1. Install packages: `cd ai-stoploss-engine-be && npm install trading-signals@^7.4.3 simple-statistics@^7.8.9` (per D-02, D-09)

    2. Create `services/ai/` directory. Create `indicatorCache.js`:
       - Import ATR, BollingerBands, SMA from 'trading-signals'
       - Map cache: symbol -> { atr (period 14), bb (20, 2), sma50 (50), sma200 (200), lastUpdate, regime, regimeTimestamp }
       - Export `getOrCreateIndicators(symbol, period=14)` -- lazy create
       - Export `feedCandle(symbol, candle)` -- stream {high, low, close} to all indicators
       - Export `clearCache()` -- for testing
       - Cache TTL 30 min (per D-08) -- stale check in getter

    3. Create `regimeDetector.js` (per D-03, D-04):
       - Export `detectRegime(indicators)`:
         - if !bb.isStable or !sma50.isStable -> return 'SIDEWAYS' (default)
         - bbResult = bb.getResult(), bbPercentile = (upper - lower) / middle * 100
         - if bbPercentile > 70 -> return 'VOLATILE'
         - if sma200.isStable: sma50 > sma200 -> 'BULLISH', sma50 < sma200 -> 'BEARISH'
         - else 'SIDEWAYS'
       - Export REGIME_MULTIPLIERS = { VOLATILE: 2.5, BULLISH: 1.5, BEARISH: 1.5, SIDEWAYS: 1.0 }

    4. Create `sectorClassification.js` (per D-19):
       - Export VN_SECTOR_MAP: hardcoded map of VN stocks -> sectors (BANKING, REAL_ESTATE, TECHNOLOGY, RETAIL, STEEL, SECURITIES, ENERGY, CONSUMER)
       - Cover at least: VCB, BID, CTG, TCB, MBB, ACB, VPB, STB, HDB, TPB (banking), VHM, VIC, NVL, KDH (real estate), FPT, CMG (tech), MWG, PNJ, DGW (retail), HPG, HSG, NKG (steel), SSI, VCI, HCM (securities), GAS, POW, PLX (energy), VNM, SAB, MSN (consumer)
       - Export SECTOR_LABELS: Vietnamese labels
       - Export getSector(symbol) -- returns sector string, default 'OTHER'

    5. Create `services/shared/priceBandValidator.js` (per D-06):
       - Import snapToTickSize from tickSizeEngine.js
       - Export getFloorPrice(refPrice, exchange='HOSE') -- refPrice * (1 - bandLimit). HOSE: 7%, HNX: 10%
       - Export getCeilingPrice(refPrice, exchange='HOSE') -- refPrice * (1 + bandLimit)
       - Export clampToBand(price, refPrice, exchange='HOSE') -- clamp price between floor and ceiling, snap to tick size
       - KHONG dung vnStockRules.ts frontend -- tao backend utility rieng
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-be && node -e "import('trading-signals').then(m => console.log('trading-signals OK:', !!m.ATR)); import('simple-statistics').then(m => console.log('simple-statistics OK:', !!m.mean))" && node -e "import('./services/ai/indicatorCache.js').then(m => console.log('indicatorCache exports:', Object.keys(m))); import('./services/ai/regimeDetector.js').then(m => console.log('regimeDetector exports:', Object.keys(m))); import('./services/ai/sectorClassification.js').then(m => console.log('sectorClassification exports:', Object.keys(m))); import('./services/shared/priceBandValidator.js').then(m => console.log('priceBandValidator exports:', Object.keys(m)))"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "trading-signals" ai-stoploss-engine-be/package.json
    - grep -q "simple-statistics" ai-stoploss-engine-be/package.json
    - grep -q "export function detectRegime" ai-stoploss-engine-be/services/ai/regimeDetector.js
    - grep -q "REGIME_MULTIPLIERS" ai-stoploss-engine-be/services/ai/regimeDetector.js
    - grep -q "export function getOrCreateIndicators" ai-stoploss-engine-be/services/ai/indicatorCache.js
    - grep -q "export function feedCandle" ai-stoploss-engine-be/services/ai/indicatorCache.js
    - grep -q "export function getSector" ai-stoploss-engine-be/services/ai/sectorClassification.js
    - grep -q "VN_SECTOR_MAP" ai-stoploss-engine-be/services/ai/sectorClassification.js
    - grep -q "export function clampToBand" ai-stoploss-engine-be/services/shared/priceBandValidator.js
    - grep -q "getFloorPrice" ai-stoploss-engine-be/services/shared/priceBandValidator.js
  </acceptance_criteria>
  <done>
    trading-signals va simple-statistics installed. 4 shared modules tao xong voi exports dung. indicatorCache stream data cho ATR/BB/SMA per symbol. regimeDetector tra ve regime tu BB percentile + SMA crossover. sectorClassification map VN stocks. priceBandValidator clamp gia trong HOSE 7%, HNX 10%.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests cho regimeDetector + indicatorCache + priceBandValidator</name>
  <files>
    ai-stoploss-engine-be/tests/services/regimeDetector.test.js,
    ai-stoploss-engine-be/tests/helpers/indicatorMocks.js
  </files>
  <read_first>
    ai-stoploss-engine-be/services/ai/regimeDetector.js,
    ai-stoploss-engine-be/services/ai/indicatorCache.js,
    ai-stoploss-engine-be/services/shared/priceBandValidator.js,
    ai-stoploss-engine-be/vitest.config.js,
    ai-stoploss-engine-be/tests/services/paperMatchingEngine.test.js
  </read_first>
  <behavior>
    - regimeDetector: BB percentile > 70 -> VOLATILE
    - regimeDetector: SMA50 > SMA200 -> BULLISH
    - regimeDetector: SMA50 < SMA200 -> BEARISH
    - regimeDetector: unstable indicators -> SIDEWAYS (default)
    - regimeDetector: REGIME_MULTIPLIERS[VOLATILE] = 2.5, [BULLISH] = 1.5, [SIDEWAYS] = 1.0
    - indicatorCache: getOrCreateIndicators tao moi hoac tra ve cached
    - indicatorCache: feedCandle updates all indicators
    - priceBandValidator: HOSE floor = refPrice * 0.93, ceiling = refPrice * 1.07
    - priceBandValidator: HNX floor = refPrice * 0.90, ceiling = refPrice * 1.10
    - priceBandValidator: clampToBand(price < floor) -> floor
    - priceBandValidator: clampToBand(price > ceiling) -> ceiling
  </behavior>
  <action>
    1. Create `tests/helpers/indicatorMocks.js`:
       - Export `createMockIndicators(overrides)` that returns object matching indicatorCache shape
       - Mock bb with configurable isStable, getResult() returning { upper, lower, middle }
       - Mock sma50, sma200 with configurable isStable, getResult() returning number

    2. Create `tests/services/regimeDetector.test.js`:
       - Test detectRegime with mock indicators cho moi regime
       - Test REGIME_MULTIPLIERS values match D-04
       - Test priceBandValidator: getFloorPrice, getCeilingPrice, clampToBand cho HOSE va HNX
       - Test indicatorCache: getOrCreateIndicators creates new, returns existing
       - Test feedCandle does not throw

    3. Run: `cd ai-stoploss-engine-be && npx vitest run tests/services/regimeDetector.test.js --reporter=verbose`
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-be && npx vitest run tests/services/regimeDetector.test.js --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "detectRegime" ai-stoploss-engine-be/tests/services/regimeDetector.test.js
    - grep -q "VOLATILE" ai-stoploss-engine-be/tests/services/regimeDetector.test.js
    - grep -q "BULLISH" ai-stoploss-engine-be/tests/services/regimeDetector.test.js
    - grep -q "SIDEWAYS" ai-stoploss-engine-be/tests/services/regimeDetector.test.js
    - grep -q "priceBand" ai-stoploss-engine-be/tests/services/regimeDetector.test.js
    - grep -q "createMockIndicators" ai-stoploss-engine-be/tests/helpers/indicatorMocks.js
  </acceptance_criteria>
  <done>
    All tests pass. regimeDetector returns correct regime cho moi case. priceBandValidator clamp dung bien do. indicatorCache tao va cache indicators.
  </done>
</task>

</tasks>

<verification>
- `cd ai-stoploss-engine-be && npx vitest run tests/services/regimeDetector.test.js --reporter=verbose` — all tests pass
- `node -e "import('./services/ai/regimeDetector.js').then(m => console.log(m.REGIME_MULTIPLIERS))"` — prints { VOLATILE: 2.5, BULLISH: 1.5, BEARISH: 1.5, SIDEWAYS: 1.0 }
- `node -e "import('./services/ai/sectorClassification.js').then(m => console.log(m.getSector('VCB')))"` — prints 'BANKING'
</verification>

<success_criteria>
- trading-signals va simple-statistics co trong package.json
- 4 shared modules export dung functions
- regimeDetector test suite pass
- priceBandValidator clamp HOSE 7%, HNX 10% dung
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-enhancement/03-01-SUMMARY.md`
</output>
