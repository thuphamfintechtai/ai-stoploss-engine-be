---
phase: 03-ai-enhancement
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - ai-stoploss-engine-be/services/ai/probabilityTP.js
  - ai-stoploss-engine-be/controllers/ai.controller.js
  - ai-stoploss-engine-be/tests/services/probabilityTP.test.js
autonomous: true
requirements: [AITP-01, AITP-02, AITP-03, AITP-04]

must_haves:
  truths:
    - "TP dua tren phan phoi log-normal tu 60-200 ngay du lieu lich su"
    - "Hien thi 3-5 muc TP voi xac suat + timeframe (vi du: 70% dat 25500 trong 5 ngay)"
    - "Probability-based TP la default suggestion, thay the ATR x RR co hoc"
    - "Label experimental cho probability-based TP"
    - "Fallback ATR x RR khi < 60 ngay du lieu"
  artifacts:
    - path: "ai-stoploss-engine-be/services/ai/probabilityTP.js"
      provides: "Log-normal probability calculation for TP levels"
      exports: ["calculateTPProbabilities"]
    - path: "ai-stoploss-engine-be/controllers/ai.controller.js"
      provides: "Updated suggestSLTP endpoint with probability TP"
    - path: "ai-stoploss-engine-be/tests/services/probabilityTP.test.js"
      provides: "Tests for probability TP calculation"
  key_links:
    - from: "ai-stoploss-engine-be/services/ai/probabilityTP.js"
      to: "simple-statistics"
      via: "import mean, standardDeviation, probit"
      pattern: "import.*simple-statistics"
    - from: "ai-stoploss-engine-be/controllers/ai.controller.js"
      to: "ai-stoploss-engine-be/services/ai/probabilityTP.js"
      via: "import calculateTPProbabilities"
      pattern: "import.*probabilityTP"
---

<objective>
Tao Probability-based Take Profit service dua tren log-normal distribution va upgrade suggestSLTP endpoint de tra ve probability TP lam default.

Purpose: User thay % xac suat dat moi muc TP thay vi chi 1 target price co hoc (ATR x RR). Label "experimental" honest about limitations.
Output: probabilityTP.js service + updated ai.controller.js + tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ai-enhancement/03-CONTEXT.md
@.planning/phases/03-ai-enhancement/03-RESEARCH.md
@.planning/phases/03-ai-enhancement/03-01-SUMMARY.md

@ai-stoploss-engine-be/controllers/ai.controller.js
@ai-stoploss-engine-be/services/aiService.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create probabilityTP.js service + tests</name>
  <files>
    ai-stoploss-engine-be/services/ai/probabilityTP.js,
    ai-stoploss-engine-be/tests/services/probabilityTP.test.js
  </files>
  <read_first>
    ai-stoploss-engine-be/services/aiService.js,
    ai-stoploss-engine-be/vitest.config.js,
    ai-stoploss-engine-be/tests/services/regimeDetector.test.js
  </read_first>
  <behavior>
    - calculateTPProbabilities(ohlcvData, currentPrice, timeframeDays=[3,5,10,20]): tra ve { levels, data_quality, experimental }
    - Khi ohlcvData.length < 60 -> return null (D-14 fallback signal)
    - levels la array of { price, probability (%), timeframe_days, label (Vietnamese) }
    - levels.length between 3 va 5 (D-11)
    - data_quality co days_used, mu_daily, sigma_daily
    - experimental = true (D-13)
    - Log returns tinh tu closes: ln(close[i] / close[i-1])
    - N-day scaling: mu_N = mu * days, sigma_N = sigma * sqrt(days)
    - Target price = currentPrice * exp(mu_N + sigma_N * z) voi z = probit(1 - pTarget)
    - Chi tra ve levels co targetPrice > currentPrice (take PROFIT, khong take LOSS)
    - Probability percentiles: 25%, 50%, 75%, 90%
  </behavior>
  <action>
    1. TAO TEST TRUOC (TDD RED): `tests/services/probabilityTP.test.js`
       - Generate mock OHLCV data (100 candles voi closes tang nhe ~0.1%/day)
       - Test calculateTPProbabilities voi 100 candles -> tra ve object voi levels, data_quality, experimental
       - Test levels co 3-5 items, moi item co price (number > currentPrice), probability (number 1-100), timeframe_days, label (string)
       - Test experimental === true
       - Test data_quality.days_used === closes.length
       - Test voi < 60 candles -> return null
       - Test voi empty array -> return null
       - Run -- MUST fail (RED)

    2. TAO SERVICE (TDD GREEN): `services/ai/probabilityTP.js`
       - Import { mean, standardDeviation, probit } from 'simple-statistics' (per D-09)
       - Export calculateTPProbabilities(ohlcvData, currentPrice, timeframeDays = [3, 5, 10, 20]):
         * Guard: if ohlcvData.length < 60, return null (D-14)
         * Tinh closes array, filter null/0
         * Tinh logReturns: ln(close[i] / close[i-1])
         * mu = mean(logReturns), sigma = standardDeviation(logReturns)
         * Loop timeframeDays x percentiles [0.25, 0.50, 0.75, 0.90]:
           - muN = mu * days, sigmaN = sigma * sqrt(days)
           - z = probit(1 - pTarget)
           - targetPrice = currentPrice * exp(muN + sigmaN * z)
           - Chi push neu targetPrice > currentPrice
           - Round price to integer (VND)
           - label = `${prob}% dat ${price.toLocaleString('vi-VN')} trong ${days} ngay`
         * Sort by probability DESC, slice(0, 5)
         * Return { levels, data_quality: { days_used, mu_daily: mu, sigma_daily: sigma }, experimental: true }

    3. Run tests -- MUST pass (GREEN)
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-be && npx vitest run tests/services/probabilityTP.test.js --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function calculateTPProbabilities" ai-stoploss-engine-be/services/ai/probabilityTP.js
    - grep -q "simple-statistics" ai-stoploss-engine-be/services/ai/probabilityTP.js
    - grep -q "probit" ai-stoploss-engine-be/services/ai/probabilityTP.js
    - grep -q "experimental.*true" ai-stoploss-engine-be/services/ai/probabilityTP.js
    - grep -q "probability" ai-stoploss-engine-be/tests/services/probabilityTP.test.js
    - grep -q "levels" ai-stoploss-engine-be/tests/services/probabilityTP.test.js
    - grep -q "< 60" ai-stoploss-engine-be/tests/services/probabilityTP.test.js
  </acceptance_criteria>
  <done>
    probabilityTP.js tinh dung xac suat cho moi muc TP tu log-normal distribution. Tra ve 3-5 levels voi probability %, timeframe, label tieng Viet. Return null khi < 60 ngay data. experimental=true. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade suggestSLTP endpoint voi probability TP lam default</name>
  <files>
    ai-stoploss-engine-be/controllers/ai.controller.js,
    ai-stoploss-engine-be/services/aiService.js
  </files>
  <read_first>
    ai-stoploss-engine-be/controllers/ai.controller.js,
    ai-stoploss-engine-be/services/aiService.js,
    ai-stoploss-engine-be/services/ai/probabilityTP.js,
    ai-stoploss-engine-be/routes/ai.routes.js
  </read_first>
  <action>
    1. Trong ai.controller.js, update suggestSLTP handler:
       - Import calculateTPProbabilities from '../services/ai/probabilityTP.js'
       - Sau khi fetch OHLCV data (da co fetchOHLCV helper), goi calculateTPProbabilities(ohlcvData, currentPrice)
       - Giu nguyen existing suggestStopLossTakeProfit call tu aiService.js cho SL calculation
       - Merge response:
         * Neu probabilityTP !== null:
           - response.take_profit_levels = probabilityTP.levels (D-11, D-12: probability-based la default)
           - response.take_profit_method = 'probability' (D-12)
           - response.take_profit_experimental = true (D-13)
           - response.take_profit_data_quality = probabilityTP.data_quality
           - Giu response.take_profit_vnd (ATR x RR) nhu backup/reference
         * Neu probabilityTP === null (< 60 ngay data):
           - response.take_profit_method = 'atr_rr' (D-14: fallback)
           - response.take_profit_warning = 'Khong du du lieu lich su (can >= 60 ngay) de tinh xac suat. Dang su dung ATR x RR ratio.'

    2. KHONG thay doi signature cua suggestSLTP controller -- van nhan cung request body
    3. KHONG thay doi suggestStopLossTakeProfit trong aiService.js -- giu nguyen, chi extend response trong controller

    4. Tang fetchOHLCV limit tu 50 -> 200 (D-10: 60-200 ngay data) trong suggestSLTP handler.
       Chi thay doi limit trong 1 cho: khi goi fetchOHLCV cho probability TP, dung limit=200.
       Existing calls khac giu limit=50.
  </action>
  <verify>
    <automated>cd ai-stoploss-engine-be && node -e "import('./controllers/ai.controller.js').then(() => console.log('Controller imports OK')).catch(e => console.log('Expected error (no DB):', e.message?.slice(0,80)))"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "calculateTPProbabilities" ai-stoploss-engine-be/controllers/ai.controller.js
    - grep -q "probabilityTP" ai-stoploss-engine-be/controllers/ai.controller.js
    - grep -q "take_profit_levels" ai-stoploss-engine-be/controllers/ai.controller.js
    - grep -q "take_profit_method" ai-stoploss-engine-be/controllers/ai.controller.js
    - grep -q "experimental" ai-stoploss-engine-be/controllers/ai.controller.js
    - grep -q "take_profit_warning" ai-stoploss-engine-be/controllers/ai.controller.js
    - grep -q "limit.*200\|200.*limit" ai-stoploss-engine-be/controllers/ai.controller.js
  </acceptance_criteria>
  <done>
    suggestSLTP endpoint tra ve probability-based TP levels lam default khi du data (>= 60 ngay). Fallback ATR x RR voi warning khi < 60 ngay. experimental=true luon duoc set. Response backward-compatible (van co take_profit_vnd cu).
  </done>
</task>

</tasks>

<verification>
- `cd ai-stoploss-engine-be && npx vitest run tests/services/probabilityTP.test.js --reporter=verbose` — all tests pass
- Existing tests still pass: `cd ai-stoploss-engine-be && npx vitest run --reporter=verbose`
- `grep "take_profit_levels" ai-stoploss-engine-be/controllers/ai.controller.js` — endpoint returns probability levels
</verification>

<success_criteria>
- probabilityTP.js tinh probability dung tu log-normal distribution
- 3-5 TP levels voi probability %, timeframe, Vietnamese label
- suggestSLTP endpoint tra ve probability TP lam default
- Fallback ATR x RR voi warning khi < 60 ngay data
- experimental=true luon set
- Backward compatible (take_profit_vnd van co)
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-enhancement/03-03-SUMMARY.md`
</output>
